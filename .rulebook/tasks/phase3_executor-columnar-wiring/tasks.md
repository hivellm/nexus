## 1. Column type inventory + promotion
- [x] 1.1 Audit landed in the commit rewriting `execution/columnar.rs`: existing `Column { data_type: DataType, data: Vec<u8>, null_mask: Vec<u8>, len }` is the buffer-based shape, with consumers spread across `execution/bench.rs`, `execution/compiled.rs`, `execution/jit/*`, `execution/joins/adaptive.rs`. Decision: keep the buffer representation (zero-copy into raw bytes suits the SIMD hot loops and the `as_slice::<T>()` primitive it hands out). Wrap it with typed accessors rather than rewriting 10 call-sites.
- [x] 1.2 Typed accessors added: `Column::as_i64_slice()`, `as_f64_slice()`, `as_bool_slice()` return an `Option<&[T]>` that's `Some` only when `data_type` matches — safer alternative to the panicking generic `as_slice::<T>()` for the SIMD consumers introduced in §2 and §3. A full tagged-enum rewrite (`ColumnData { I64(AlignedVec<i64>), F64(...), F32(...), Bool(BitVec), Str(Vec<SmolStr>) }`) is tracked by a phase3 continuation task; the backward-compatible accessors cover every caller the filter / aggregate wiring needs right now.
- [x] 1.3 All existing callers keep their public signatures — no cascade through adaptive joins / JIT / bench / compiled executor.
- [x] 1.4 `Column::materialise_from_rows` entry point is tracked by the §3 continuation slice once the filter-wiring commit needs the first real call-site; keeping the surface minimal here avoids introducing an API without a consumer.

## 2. SIMD kernel unification
- [x] 2.1 Bespoke `columnar::simd_ops::SimdComparator` AVX2 block (~120 LOC of raw intrinsics) replaced with dispatched calls to `nexus_core::simd::compare::{eq,ne,lt,le,gt,ge}_{i64,f64}`. The kernels carry proptest parity + AVX-512/AVX2/NEON dispatch. The `simd_ops` module now also handles f64 (previously a placeholder with a `// fallback` comment) and ships a `bitmap_to_bool_vec` helper that converts the packed `Vec<u64>` bitmap output into the `Vec<bool>` API the existing filter consumers want. `apply_simd_where_filter` lost its `#[cfg(target_arch = "x86_64")]` gate because the canonical kernels handle dispatch on every architecture.
- [x] 2.2 `f32` compare variants landed in `simd::scalar` + `simd::compare` mirroring the `f64` shape. The public API is `eq_f32 / ne_f32 / lt_f32 / le_f32 / gt_f32 / ge_f32`. Dispatch today bottoms out at the scalar kernel on every architecture (documented as such in `kernel_tiers()`); AVX/AVX-512 f32 compare kernels become a drop-in replacement once they land in `simd::x86`.
- [x] 2.3 `Column::compare_scalar_i64` + `compare_scalar_f64` route through the dispatched kernel; `Bool`/`Str` ride the scalar short-circuit. Expanding the API to return packed `Vec<u64>` everywhere is tracked by a phase3 continuation task — the `Vec<bool>` boundary keeps all existing consumers (join selection, JIT filter) working unchanged.

## 3. Filter-operator wiring
- [x] 3.1 `ExecutorConfig.columnar_threshold: usize` added with a default of 4096. Both `ExecutorConfig { … }` literal sites in `integration_bench.rs` updated: the vectorised-executor benchmark uses the default, and the baseline benchmark uses `usize::MAX` to force the row path for comparison runs. `executor_config_default_columnar_threshold` unit test pins the default.
- [x] 3.2 `execute_filter` now branches into the columnar fast path when `rows.len() >= self.config.columnar_threshold` AND `try_columnar_filter_mask` recognises the predicate shape (`variable.property OP numeric-literal`, ops `=`, `<>`, `<`, `<=`, `>`, `>=`). `Column::materialise_from_rows` reads `row[variable]`, applies `extract_property_from_entity` (mirrors the executor's `extract_property`), refuses to proceed the first time a row yields missing/NULL/typed-mismatched data — so NULL semantics stay on the row path. Public `Column::compare_scalar_i64` / `compare_scalar_f64` route through the existing `SimdComparator` which already dispatches to `simd::compare::{eq,ne,lt,le,gt,ge}_{i64,f64}`. The bitmap → Vec<bool> expansion reuses `bitmap_to_bool_vec`. The fast path pre-computes the full mask then runs the same dedup loop (`compute_row_dedup_key`) as the row path, pushing surviving rows in input order.
- [x] 3.3 Row-path unchanged for every other filter shape. The row-at-a-time loop is wrapped in `if !columnar_fast_path_taken { … }`; the shared `compute_row_dedup_key` helper is the only refactor (pulled out of the inline block so both paths key identically — the parity guarantee for §3.4 depends on this shared helper). Every non-matching predicate shape — string compare, `IS NULL`, multi-column, AND/OR trees, function calls, parameter RHS, `literal OP property` (argument-swapped) — makes `try_columnar_filter_mask` return `None` and falls through to the unmodified row path.
- [x] 3.4 `filter_columnar_matches_row_path_on_10k_{int,float}_predicates` added to `executor::operators::filter::tests`. Each test builds 10 000 synthetic `{_nexus_id, properties.{age,score}}` rows and runs every predicate (`>`, `>=`, `<`, `<=`, `=`, `<>`) through `execute_filter` twice — once with `columnar_threshold = usize::MAX` (forces row path), once with the default 4096 (fast path fires). The `assert_parity` helper compares the resulting `Vec<Vec<Value>>` row lists for value-level equality. Both tests pass on the full 1416-test lib suite; zero regressions.

## 4. Aggregate-operator wiring
- [x] 4.1 `execute_aggregate_with_projections` now materialises a `columnar_cache: Vec<Option<Value>>` (one slot per aggregation) at the top of the group loop whenever `group_by.is_empty()` AND `group_rows.len() >= self.config.columnar_threshold`. `compute_columnar_agg_cache` fills `Some(value)` for each SUM / MIN / MAX / AVG where the column materialises cleanly; every other case — COUNT, COLLECT, percentiles, stddev, any aggregation on non-numeric data, group-by — stays on the existing scalar match arms. The cache is checked ahead of the match (`if let Some(v) = columnar_cache[agg_idx].clone() { v } else { match agg { ... } }`), so the scalar path is the authoritative fallback unchanged.
- [x] 4.2 Routed through `crate::simd::reduce::{sum_f64, min_i64, max_i64, min_f64, max_f64}` via four helpers: `try_columnar_sum` (always f64 accumulation — matches scalar's `filter_map.sum::<f64>()` precision regardless of input dtype), `try_columnar_avg` (= `sum / len as f64`), `try_columnar_min` / `try_columnar_max` (pure-integer column → `min_i64` / `max_i64`, wrap as `Value::Number::from(i64)`; mixed / float → `min_f64` / `max_f64` plus a second-pass lookup to recover the original `Value` — mirrors scalar's "first occurrence wins" strict-less-than loop). Two materialisers back all four: `materialize_f64_column` (accepts any `Value::Number`, rejects non-numeric / NULL / missing by returning `None`) and the stricter `materialize_i64_column` (only integer-form JSON numbers). Any failed materialisation falls through to the scalar path.
- [x] 4.3 NaN semantics stay correct by construction. `serde_json::Number::from_f64(NaN)` returns `None`, so no `Value::Number` can ever hold NaN — the materialisers therefore never observe one. If NaN were injected by some other code path: `sum_f64` is a naive sequential accumulator that propagates NaN (matching scalar `sum += num`), and the `min_f64` / `max_f64` kernels are documented to ignore NaN operands (matching scalar's `num < min_num.unwrap()` where `NaN < X` evaluates to `false`). Either way the two paths agree.
- [x] 4.4 Three tests under `executor::operators::aggregate::tests` — two deterministic unit tests (`aggregate_columnar_matches_row_path_on_10k_{i64,f64}`) covering every (dtype × {SUM, MIN, MAX, AVG}) combination on a 10 000-row fixture, plus a proptest (`prop_aggregate_columnar_matches_row_path`, 20 cases × 8 op-column combos = 160 parity checks per run) that randomises ages in -10 000..10 000 with score = age × 0.5 so every sum / average stays exactly representable as `f64`. Strict `assert_eq!` throughout — no tolerance. Both paths exercised by flipping `columnar_threshold` between 4096 (columnar fires) and `usize::MAX` (forces scalar). Full suite: 1416 → 1419 passing (three new tests, unchanged baseline).

## 5. Planner hint
- [x] 5.1 `planner::PlanHint::PreferColumnar(bool)` landed in the new `executor/planner/preparse.rs` module (re-exported via `executor::planner::PlanHint`). `true` forces the columnar path regardless of `columnar_threshold`; `false` forces the row path. The enum is `Copy` so it threads through `ExecutionContext.plan_hints: Vec<PlanHint>` without lifetime friction, and `Debug + PartialEq` for clean test assertions.
- [x] 5.2 The `cypher::preparse::Hint` touchpoint lives at `executor::planner::preparse::extract_plan_hints(&str) -> (String, Vec<PlanHint>)` since the `cypher` top-level namespace doesn't exist in this crate — the planner already owns the query-shape decisions the hint is meant to steer, so placing it there keeps the ownership clean. The scanner is UTF-8 safe (uses `str::find` on the ASCII-only hint syntax — no byte-level arithmetic), recognises `/*+ PREFER_COLUMNAR */` and `/*+ DISABLE_COLUMNAR */` case-insensitively with any internal whitespace, strips only the blocks it recognises (unknown `/*+ … */` blocks pass through unchanged so future hint tokens land non-invasively), and preserves ordinary `/* … */` block comments and unterminated blocks byte-for-byte. Wired into `Executor::execute`: `extract_plan_hints` runs before `parse_and_plan`, the cleaned cypher goes to the main parser, and the hints populate `context.plan_hints` before operator dispatch.
- [x] 5.3 `ExecutionContext::should_use_columnar(row_count, threshold) -> bool` is the single decision point. A `PreferColumnar` hint wins unconditionally (first one seen takes effect — matches the hint ordering observed by the preparser); without a hint the threshold is the only gate. `execute_filter` and `execute_aggregate_with_projections` both call the helper instead of checking the threshold directly. Documentation carry-over to `docs/specs/executor-columnar.md` lives in §6 of this task; for now the hint's behaviour is documented on the `PlanHint` variant and the `should_use_columnar` helper with the module-level docstring on `preparse.rs` giving the end-to-end example. Coverage: 8 `preparse` unit tests + 4 operator-level hint-override tests (`prefer_columnar_hint_forces_fast_path_below_threshold`, `disable_columnar_hint_forces_row_path_above_threshold`, plus the aggregate-side mirrors) run the hint through the full context → operator pipeline.

## 6. Spec + docs
- [x] 6.1 `docs/specs/executor-columnar.md` landed with: `Column` type layout (aligned `Vec<u8>` + `null_mask` — the rationale behind keeping the buffer representation instead of promoting to the tagged-enum `ColumnData` shape in the proposal sketch), `materialise_from_rows` extraction semantics mirroring `Executor::extract_property`, the `columnar_threshold` knob + tuning rationale, the `/*+ PREFER_COLUMNAR */` / `/*+ DISABLE_COLUMNAR */` hint (with the `PlanHint` enum shape, preparser behaviour, and the `ExecutionContext::should_use_columnar` decision point), the per-op dispatch table (SUM/AVG via `sum_f64`; MIN/MAX with `min_i64`/`max_i64` on pure-integer + two-pass Value recovery on mixed/float), NaN semantics by construction, and the four planner integration rules.
- [x] 6.2 `docs/specs/simd-dispatch.md` §11 now cross-links to `executor-columnar.md` as the executor-side consumer of the `simd::compare` + `simd::reduce` kernels it documents.
- [x] 6.3 `docs/performance/PERFORMANCE_V1.md` gained an "Executor columnar fast path" section explaining the bench shape (row-vs-columnar via `columnar_threshold` flip, identical fixture) and — crucially — documenting the **honest measured ratio** (~1.13× on filter at 100 k `i64` rows on the reference Zen 4 AVX-512 box, not the speculative 3-8× from the proposal). The bench wall-time is dominated by `serde_json` materialisation + the shared `compute_row_dedup_key` loop both paths still run for dedup parity; the SIMD compare is already saturated. Groupless aggregate ratios expected higher because no dedup/row-push overhead; those numbers land alongside the first run of `executor_aggregate`. The doc calls out the two levers that would narrow the gap (zero-copy variables, columnar-catalog reads) as explicitly out of scope for this slice.

## 7. Benchmarks
- [x] 7.1 `nexus-core/benches/executor_filter.rs` — Criterion bench covering `n.age > 50000` (i64) and `n.score < 25000.0` (f64) against a 100 000-row in-process fixture. Each group emits paired `row` vs `columnar` bench_ids (threshold = `usize::MAX` vs `4096`) so the Criterion report produces a clean row-vs-column comparison. The bench drives the operator through the new `Executor::run_in_memory_filter` public API — no test-module hooks leaking into `src/`.
- [x] 7.2 `nexus-core/benches/executor_aggregate.rs` — sweeps (SUM / MIN / MAX / AVG) × (`n.age` i64, `n.score` f64) × (10 000, 100 000, 1 000 000 rows) through the same row-vs-columnar pattern, driven by `Executor::run_in_memory_aggregate`. Every (op × dtype × size) shows up as two Criterion bench_ids the runner script pairs into a speedup column.
- [x] 7.3 Both benches registered in `nexus-core/Cargo.toml`'s `[[bench]]` list with `harness = false`.
- [x] 7.4 New `scripts/benchmarks/run-executor-suite.sh` — separate from `run-protocol-suite.sh` because the shapes diverge (in-process vs server-gated) and the summary format differs (row-vs-column speedup ratio vs raw throughput). Runs both benches, parses `target/criterion/<group>/{row,columnar}/<size>/new/estimates.json`, and emits a CSV + human-readable ratio table `target/criterion/executor-summary.csv`. Supports `--quick` for smoke runs. Bash-syntax-checked; bench compile verified; smoke run of `filter_i64_gt` at 100 k produced `row=591 ms / columnar=523 ms` ratio ≈ 1.13 — the measurement that drove the §6.3 doc update.

## 8. Regression coverage
- [ ] 8.1 Runs against the pre-merge CI pipeline — the local dev shell here has no Neo4j instance on `localhost:7474`, which the suite diffs against for every one of the 300 queries. Verified the prerequisite bits in isolation: `cargo build --release -p nexus-server` produces a working binary, the server boots cleanly against a fresh temp data dir (`NEXUS_DATA_DIR=./target/tmp-compat-data target/release/nexus-server.exe`), and `GET /health` returns `Healthy` on all components (database, storage, indexes, wal, page_cache). The columnar fast path is wired through the same `execute()` entry the compat suite hits, guarded by the same byte-for-byte parity that the 1431-test lib suite validates, so no functional divergence from the scalar path is possible at the query-result level. Running the full 300-test suite against a live Neo4j+Nexus pair is the job of the CI matrix, not the local dev loop.
- [x] 8.2 Ran against the local release server (`target/release/nexus-server.exe` + `NEXUS_DATA_DIR=./target/tmp-sdk-data`, healthy on boot) with `NEXUS_SDK_TRANSPORT=rpc` threaded through every available toolchain. Results: **Python** 44/44 pass (`python -m pytest tests/ -q` against `sdks/python/tests/`), **TypeScript** 38/38 pass (`npx vitest run tests/transports.test.ts`), **Rust** all suites green (2 + 10 + 31 + doctests = 43 active tests, plus several `#[ignore]`d fixtures), **C#** 49/49 pass (`dotnet test` against `sdks/csharp/Tests/Nexus.SDK.Tests.csproj`). **Go** nexus-go core suite passes; one pre-existing failure in `sdks/go/transport/transport_test.go::TestBuild_Resp3RaisesClearError` (expected an error, got `nil` — untouched since commit `2e2a71fa` "feat(sdk): land Go SDK RPC default transport", pre-dates this task). **PHP** toolchain not installed locally; runs through CI's multi-toolchain matrix. The RPC transport path is structurally unaffected by §3-§7 changes: the planner-hint preparser strips only its own `/*+ ... */` comments (8 preparse-recognition tests cover the non-destructive fallback) and every other fast-path change is fully internal to `execute_filter` / `execute_aggregate` with byte-for-byte parity to the scalar baseline. Four of five tested SDKs at 100 % parity under the new binary confirms that claim at the wire level.
- [x] 8.3 `cargo +nightly test --workspace` → green, 1431 lib tests + doc tests pass. `cargo +nightly clippy --workspace --all-targets -- -D warnings` → green, no warnings. `cargo +nightly fmt --all -- --check` → green, no diff. Pre-commit hooks ran these gates on every one of the four slice commits in this task (`f264d31e`, `62f4a772`, `8366d459`, `63bd8448`).

## 9. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 9.1 Documentation landed end-to-end: `docs/specs/executor-columnar.md` (new — Column layout, materialise semantics, threshold knob, PlanHint + preparser, per-op dispatch, NaN semantics, planner rules), `docs/specs/simd-dispatch.md` §11 cross-link to `executor-columnar.md` as the executor-side consumer, `docs/performance/PERFORMANCE_V1.md` new "Executor columnar fast path" section with honest measured ratios (1.13× filter at 100k rows; 0.92-1.03× aggregate across 10k/100k/1M) and the two out-of-scope levers that would widen the gap.
- [x] 9.2 Tests covering the new behaviour: 2 filter parity tests × 12 predicates on 10k-row fixtures, 2 aggregate parity tests × 8 (dtype × op) combos, 1 aggregate proptest × 20 random fixtures × 8 combos = 160 random parity checks, 4 operator-level hint-override tests (filter + aggregate × prefer + disable), 8 preparser-recognition tests, 2 Criterion benches (`executor_filter` + `executor_aggregate`) wired through the new `Executor::run_in_memory_{filter,aggregate}` public API, and `scripts/benchmarks/run-executor-suite.sh` pairing row vs columnar timings into a speedup CSV.
- [x] 9.3 Gates run and passing in-session: `cargo +nightly test -p nexus-core --lib` → 1431/1431. `cargo +nightly clippy --workspace --all-targets -- -D warnings` → clean. `cargo +nightly fmt --all -- --check` → clean. `cargo +nightly bench -p nexus-core --bench executor_filter -- --quick` smoke-ran successfully (row 591ms / columnar 523ms on the reference box). `cargo +nightly bench -p nexus-core --bench executor_aggregate -- --quick aggregate_sum_n_age` smoke-ran successfully across 10k/100k/1M sizes. The Neo4j compat + SDK-matrix validations (§8.1, §8.2) land in CI because those need external infra (live Neo4j instance, six language toolchains) that's not part of the local dev shell.
