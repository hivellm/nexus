## 1. Implementation
- [x] 1.1 Standalone CREATE evaluates function-call property values (route through the row-aware evaluator + canonicalize_value_in_place before storage; keep rejection only for genuinely unsupported expression classes). Review round added: `semantic_validation` now walks `Clause::Create` property maps (undefined-variable references — TCK Create1[20]/Create2[24] now PASS — and misplaced aggregates); both CREATE resolvers reuse that walk as defense-in-depth against a bypassed validation entry point; a strict function-name dispatch (`eval_function_call_strict`) rejects an unrecognized function name instead of silently nulling; a shared primitive/array-of-primitives guard rejects a resolved value that is a whole node/relationship or a literal map.
- [x] 1.2 Audit MERGE and FOREACH property paths for the same limitation; verify Temporal5.feature setup fixtures now execute (7 scenarios unblocked). Findings: MERGE's own property values (`engine/write_exec/merge.rs`) and the CREATE-in-FOREACH shape both route through `engine::match_exec::expression_to_json_value` / `engine::write_exec::dispatch::eval_write_value` — the ENGINE-level write path, a sibling module to `executor` — which cannot reach the executor's `pub(in crate::executor)` row-aware evaluator without a visibility-boundary decision (documented, left as-is; not cheap). Separately, `ForeachUpdateClause` has no `Create` variant at all — CREATE-in-FOREACH is a missing feature, not a function-call-property limitation, so there was nothing to audit there. Temporal5.feature's 7 scenarios now execute their `Given`/fixture CREATE past setup (confirmed via the TCK failure-log diff); they still fail on the documented, separate read-side gap (`phase21_tck-temporal-projection-and-store`) — this task's own dependency note already flagged that as out of scope.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation (doc comments on `properties.rs`, `semantic_validation.rs`'s new checks and `pub(in crate::executor)` reuse rationale, `eval_function_call_strict`)
- [x] 2.2 Write tests covering the new behavior (`tests/cypher/test_create_function_call_properties.rs` — 16 tests; 3 new `semantic_validation` unit tests)
- [x] 2.3 Run tests and confirm they pass (cypher/regression/compatibility/executor suites green; clippy clean; TCK: Create1[20]/Create2[24] flip to pass and stay passing across repeat runs; the round-1 net TCK gain (87) holds — the only apparent losses were confirmed pre-existing run-to-run nondeterminism, not caused by these changes, see report)
