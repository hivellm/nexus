## 1. Implementation
- [x] 1.1 full ISO date parsing — new eval/temporal_parse.rs: calendar (YYYY-MM-DD/YYYY-MM/YYYY), ISO week (YYYY-Www[-D], week-based year via chrono from_isoywd_opt), ordinal (YYYY-DDD), extended + compact notation with a single has-hyphen decision preventing mixed forms; checked slicing throughout (non-ASCII safe); date() string arm rewired from the old %Y-%m-%d-only chrono path (deliberate narrowing: non-padded dates now Null per fixed-width ISO fields). All 11 TCK Temporal2 [1] date rows pass byte-exact
- [x] 1.2 ISO duration parsing + unit carry — duration('P...') component grammar (strict unit order, max one each) + alternative P<date>T<time> form (extended and compact); fractional values on any component carry down to seconds+nanos (P5M1.5D → P5M1DT12H; mean-month 2_629_746 s only for month→day, TCK-forced); per-component and leading signs composing; sign applied in the f64 pipeline before casting with a checked f64→i64 helper — out-of-range returns Null, never panics (i64::MIN negation) or saturates; feeds make_duration which owns normalization. All 7 TCK Temporal2 [7] duration rows pass byte-exact; independent opus review APPROVE after 2 rounds (round 1 BLOCKER overflow panic + MAJOR saturation fixed), reviewer fuzzed 200k inputs with zero panics

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — CHANGELOG entry (including the padding-narrowing note); parser module docs state the un-normalized nanos contract; OPENCYPHER_TCK_REPORT.md regenerated
- [x] 2.2 Write tests covering the new behavior — 19 unit tests in temporal_parse.rs (parse ladder, carry math, mixed-notation rejection, overflow repros → Null) + 14 integration tests in tests/cypher/temporal_iso_parse_test.rs (TCK cases end-to-end, compact alternative form, negative durations, invalid → Null)
- [x] 2.3 Run tests and confirm they pass — temporal_parse unit 19/19; cypher suite green (487+ incl. all temporal); compatibility 245/245; clippy -D warnings clean; rustfmt clean; TCK 1129 → 1152 with this changeset (expressions/temporal 57 → 75; all 18 target scenarios cleared from the failure log)
