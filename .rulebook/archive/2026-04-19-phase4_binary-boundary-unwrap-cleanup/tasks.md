## 1. Implementation
- [x] 1.1 Run `rg -n '\.unwrap\(\)|\.expect\(' nexus-cli/src nexus-server/src` and catalogue each site with a verdict (fix / keep-with-comment / remove) — audit found 5 `.unwrap()` in `nexus-cli/src/commands/mod.rs` (3 on compile-time `ProgressStyle` templates, 2 on `serde_json::to_string_pretty(&Value)`); 0 in `nexus-cli/src/main.rs`; the `SystemTime` unwraps the proposal cited in `nexus-server/src` had already been cleaned up during phase2 and no hits remain.
- [x] 1.2 Replace actionable `.unwrap()` in `nexus-cli/src/commands/mod.rs`: template-string unwraps upgraded to `.expect("...literal template...")` with an invariant comment; `serde_json::to_string_pretty` unwraps switched to `match Ok => println! / Err => eprintln!` so the CLI never panics on serialisation failure.
- [x] 1.3 Fix `let _ = std::fs::create_dir_all(parent)` at `query.rs:33` to log on failure via `eprintln!("warning: ...")`; history persistence stays best-effort but no longer silent.
- [x] 1.4 Fix `let _ = rl.load_history(...)` at `query.rs:307` to print a warning on read error; missing file still passes silently (the common case).
- [x] 1.5 `SystemTime` arithmetic sweep — no `.unwrap()` hits in `nexus-server/src/` duration_since code.

## 2. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 2.1 Update or create documentation covering the implementation: new "Binary-Boundary Error Handling" sub-section in `AGENTS.override.md` documents the three canonical patterns (propagate with `?` + `anyhow::Context`, explicit `.expect("...invariant...")`, explicit `match` for best-effort side effects).
- [x] 2.2 Write tests covering the new behavior: added `scripts/ci/check_no_unwrap_in_bin.sh` which greps for `.unwrap()` under `nexus-cli/src/main.rs`, `nexus-cli/src/commands/`, and `nexus-server/src/main.rs` (production code only — `awk` strips the trailing `#[cfg(test)] mod tests { ... }` block so test-module unwraps don't trip the guard); script reports hits and exits 1 on violation, 0 when clean. Verified against a synthetic injection (exit 1) and the real repo (exit 0).
- [x] 2.3 Run tests and confirm they pass: `cargo +nightly clippy -p nexus-cli -p nexus-server -p nexus-core --all-targets --all-features -- -D warnings` (clean); `cargo +nightly test -p nexus-cli` → 36/36 pass; `cargo +nightly test -p nexus-server --lib` → 357/357 pass; `bash scripts/ci/check_no_unwrap_in_bin.sh` → exit 0.
