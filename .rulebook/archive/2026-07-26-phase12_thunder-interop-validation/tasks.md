# Tasks: phase12_thunder-interop-validation

Release gate for the Thunder migration (phases 10–11): cross-language interop
matrix + legacy-wire compatibility + regression proof on untouched surfaces.
Adapted from Synap `scripts/interop/` (its 1.2.0 gate).

## 1. Harness
- [x] 1.1 Create `scripts/interop/server-config.yml` (off-default ports, e.g. HTTP 25474 / RPC 25475 / RESP3 25476; auth REQUIRED with a fixed root user + api key) and `scripts/interop/run-matrix.py`: boot `nexus-server` from that config, run each language cell, render pass/fail matrix, exit non-zero on any FAIL; per-cell toolchain override env `NEXUS_INTEROP_<CELL>`, missing toolchain → explicit SKIP — **done**: config keeps ports 25474/25475/25476 with auth required; run-matrix boots nexus-server from it via the env vars the server actually reads, owns the lifecycle, renders the matrix, exits non-zero on red; `NEXUS_INTEROP_<CELL>` override + SKIP-on-missing-toolchain wired
- [x] 1.2 Define the client contract in `scripts/interop/README.md`: `argv: <host> <port> <user-or-key> <pass>`, stdout `STEP <name> PASS|FAIL <detail>`, exit 0 on all-pass; steps: `auth` (STATS gated before/after AUTH — PING answers pre-auth), `cypher` (CREATE + MATCH round-trip), `knn_bytes` (raw f32-LE Bytes == Array<Float>, byte-exact), `error` (typed server error, connection stays usable) — **done**: README documents the contract + the four steps

## 2. Language cells (independent — parallelizable)
- [x] 2.1 `scripts/interop/clients/rust/` — drives the Rust SDK transport directly — **done**: green (auth/cypher/knn_bytes/error)
- [x] 2.2 `scripts/interop/clients/python/` — drives `nexus_sdk` transport + thunder_rpc credentials directly — **done**: reference cell, verified green end-to-end (auth/cypher/knn_bytes/error) via `run-matrix.py python`
- [x] 2.3 `scripts/interop/clients/typescript/` — drives the TS SDK transport directly — **done**: green
- [x] 2.4 `scripts/interop/clients/go/` — drives the Go SDK transport directly — **done**: green
- [x] 2.5 `scripts/interop/clients/csharp/` — drives the C# SDK transport directly — **done**: green
- [x] 2.6 `scripts/interop/clients/php/` — drives the PHP SDK transport directly — **done**: green (Windows/winget: launch via `php-win.exe`, see README gotcha)

## 3. Backward compatibility
- [x] 3.1 `scripts/interop/clients/legacy/` — replays the pre-Thunder Nexus wire (map-shaped Request frames, Bytes as int-array) against the new server and asserts correct decode + execution; documents any accepted casualties (Synap's: legacy pub/sub-over-RPC) — **done**: green. The wire is accepted intact (server's rmp-serde tolerates map-shaped Request frames = WIRE-013, and Bytes-as-int-array = WIRE-011); the four steps all pass, so there are **no casualties** to document for Nexus (unlike Synap's pub/sub-over-RPC) — recorded in the cell docstring

## 4. Untouched-surface regression proof
- [x] 4.1 Re-run the Neo4j HTTP compat suite (`scripts/compatibility/test-neo4j-nexus-compatibility-200.ps1`) — 300/300 must hold — **done, with an honest finding**: the suite (grown to **325** cases since the 300 milestone at a9c86b27, +25 spatial diff scenarios at c10a19f0) reports **308 passed / 2 failed / 15 skipped** against a live Neo4j 5. The 2 failures are **15.08** (`MERGE .. MERGE .. RETURN a, b` → "Multiple different variables in RETURN not supported for write queries") and **15.12** (`MERGE .. WITH n MERGE .. RETURN` → "Unsupported clause in write query`") — **pre-existing Cypher-executor write-path limitations**, not Thunder regressions: the guard predates 2026-06-09 (aeb94dd3), long before the Thunder migration, and §4.2 + a direct cross-transport run confirm HTTP and RPC(Thunder) return the *identical* executor rejection. The migration regressed nothing on the HTTP surface. The two MERGE gaps are out of scope here (executor, not transport) → worth a separate write-path task
- [x] 4.2 Re-run `scripts/compatibility/test-transport-parity.sh` (HTTP vs RPC vs RESP3 envelopes still identical) and a RESP3 smoke against the migrated server — **done**: **7/7 OK, 0 divergent** — HTTP == RPC(Thunder) == RESP3 produce identical `{columns, rows}` envelopes across the write-path battery; the RESP3 leg (dockerized `redis-cli --json` → `host.docker.internal:15476`) is the smoke. Direct proof that the Thunder RPC front-end matches the untouched HTTP/RESP3 paths

## 5. Tail (docs + tests — check or waive with tailWaiver)
- [x] 5.1 Update or create documentation covering the implementation (record the full matrix + legacy cell + regression results in `docs/protocol/thunder-interop-matrix.md`; finalize `docs/specs/rpc-wire-format.md` / `docs/specs/sdk-transport.md` / `docs/specs/api-protocols.md` as post-migration state; CHANGELOG release notes) — **done**: created `docs/protocol/thunder-interop-matrix.md` (matrix 7/7 + legacy + regression proof); added the `phase12` CHANGELOG entry under 3.0.0; fixed the stale `nexus_protocol::rpc::{DEFAULT_MAX_FRAME_BYTES,PUSH_ID}` → `thunder::…` symbols and the pre-Thunder per-SDK codec list in `rpc-wire-format.md`; attributed the RPC section of `api-protocols.md` to Thunder wire v1; `sdk-transport.md` was already post-migration-current (no change). (Pre-existing: `api-protocols.md`'s "Client Libraries" REST examples still reference the removed `nexus-protocol` / a "Python V1" — broad SDK-doc drift, orthogonal to the transport migration, left for a dedicated docs refresh.)
- [x] 5.2 Write tests covering the new behavior (the interop matrix itself is the test artifact — wire it into CI as a workflow or documented manual gate so future transport changes re-run it) — **done as a documented manual gate**: `scripts/interop/README.md` and `docs/protocol/thunder-interop-matrix.md` both declare the matrix the required gate for any RPC-transport or SDK-transport change, with the exact run command. (A full multi-toolchain+submodule CI job was deliberately not hand-authored unverified — a red CI file is worse than a documented gate; the driver's graceful SKIP-on-missing-toolchain keeps a future CI wiring cheap.)
- [x] 5.3 Run tests and confirm they pass (matrix all-PASS incl. legacy cell; Neo4j suite 300/300; transport parity green; workspace gate: `cargo +nightly fmt --all`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo +nightly test --workspace`) — **done, all green**: interop matrix **7/7** incl. legacy; transport parity **7/7 OK / 0 divergent**; Neo4j differential **308/325** (the 2 MERGE failures are pre-existing executor write-path gaps, not regressions — see §4.1, not the literal "300/300" but the honest post-migration state). Workspace gate: `fmt --all --check` clean; `clippy --workspace --all-targets --all-features -- -D warnings` 0 warnings; `test --workspace` **5167 passed / 0 failed / 92 ignored** (one transient rustdoc `E0463 can't find crate for serde` during the concurrent workspace run was a build-artifact race — `--workspace --doc` re-run is **16 passed / 0 failed**; nexus-bench has no doctests and is untouched by phase12). Zero Rust workspace code changed in phase12
