## 1. Implementation
- [x] 1.1 truncate() across kinds and units — new eval/temporal_truncate.rs (569 lines): date/datetime/localdatetime/time/localtime .truncate(unit, value[, overrides]) derived row-by-row from Temporal9.feature. Units millennium..microsecond with the popular century convention the TCK pins (century(2000)=2000) and div_euclid flooring for negative years; week → Monday of the input's ISO week, weekYear → Monday of week 1 of the ISO week-YEAR (1984-01-01 → 1983-01-03); map overrides with day-replace, dayOfWeek re-anchor (documented precedence over day), and nanosecond ADDING to the truncated remainder (TCK-pinned .645000002), bounded to the sub-unit the truncation freed; offsets kept from time/datetime sources, UTC default otherwise, timezone overrides via shared temporal_retag::resolve_timezone_string (named zones error until the timezone-database task). Robustness beyond TCK coverage (review round): date-less sources vs date-bearing targets → Null (no fabricated 0000-01-01); unit×kind validity matrix enforced with hard errors; uniform override validation (key-present-but-invalid always errors). Dispatch via 5 lowercase dotted arms in fn_temporal.rs (mirroring the duration.between pattern). Independent opus review: APPROVE — every TCK-pinned semantic re-derived by hand and probed live. TCK: Temporal9 322 → 31 failures (all 31 = named-timezone rows), expressions/temporal 318 → 609 (60.7%), overall 1445 → ~1734 (44.8%)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — module/function doc comments (incl. the bounded nanosecond-carry contract and dayOfWeek precedence); CHANGELOG entry; knowledge-base pattern recording the nanosecond-adds discovery; OPENCYPHER_TCK_REPORT.md regenerated
- [x] 2.2 Write tests covering the new behavior — 29 unit tests in temporal_truncate.rs (truncation table per unit, overrides, validity matrix, bounds) + 2 in temporal_retag.rs (shared timezone resolver) + 18 integration tests in tests/cypher/temporal_truncate_test.rs (one per representative TCK row across all 5 scenarios + named-timezone hard error)
- [x] 2.3 Run tests and confirm they pass — lib truncate 29/29 + retag 21/21; cypher 604/604; compatibility 245; executor 272; regression 227; clippy -D warnings clean; rustfmt clean; TCK double-run: expressions/temporal invariant at 609/1004 both runs, Temporal9 exactly 31 timezone-blocked failures, merge/return back at baseline on the confirming run (documented coin-flips)
