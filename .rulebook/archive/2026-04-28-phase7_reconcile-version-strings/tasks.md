## 1. Implementation
- [x] 1.1 Audited current versions: workspace `Cargo.toml [workspace.package].version = "1.15.0"`; README badge was `v1.13.0` (stale); CHANGELOG top heading was `## [1.2.0] — 2026-04-28` (typo from commit 7701ae6e); all six SDK manifests at `1.15.0`. Workspace bump to `1.15.0` was deliberate (commit bcab3e21 "align server image with SDKs"); the `[1.2.0]` and `v1.13.0` references were drift, not decoupled trains.
- [x] 1.2 Decided canonical policy: **single train** for workspace + all six SDKs + protocol crate. Documented in new `docs/development/RELEASE_PROCESS.md`. Branch-name lag (`release/v1.2.0` carries `1.13`–`1.15` workspace bumps) is the one residual artefact and is documented next to the affected CHANGELOG heading; cutting a new branch every minor bump would churn upstream PR refs without benefit.
- [x] 1.3 Created `docs/COMPATIBILITY_MATRIX.md` with the current server↔SDK↔protocol mapping (everything at 1.15.0), the compatibility rules (exact-match SDK↔server in the test matrix, protocol negotiates down via the wire-format version byte, persisted data is forward-compatible within a major), and an "Older releases" table seeded with 1.15.0 / 1.13.0 / 1.0.0.
- [x] 1.4 Updated README badge `v1.13.0` → `v1.15.0`; "Highlights (v1.13.0)" heading → "Highlights (v1.15.0)"; SDK install line `nexus-sdk = "1.13.0"` → `"1.15.0"`; "tracking the same 1.13.0 line" → "1.15.0 line"; SDK comparison table cell "same 1.13.0 cadence" → "1.15.0"; Roadmap "1.13.0 — current (2026-04-22)" → "1.15.0 — current (2026-04-28)"; openCypher matrix preamble "in Nexus v1.13.0" → "v1.15.0". CHANGELOG top heading `## [1.2.0]` → `## [1.15.0]` with an in-place note explaining the typo + the branch-name retention rationale.
- [x] 1.5 Added a "**Compatibility:** SDK 1.15.0 ↔ `nexus-server` 1.15.0" callout at the top of every SDK README (`sdks/{rust,python,typescript,go,csharp,php}/README.md`), each linking to `docs/COMPATIBILITY_MATRIX.md`.
- [x] 1.6 Added `scripts/ci/check_version_consistency.sh` (executable). Parses workspace version from `Cargo.toml`, README badge from line 8, top CHANGELOG heading from `## [X.Y.Z]`, and fails with a structured diff message if any of the three drift. Local run reports `OK: all surfaces report 1.15.0`.
- [x] 1.7 Wired the script into `.github/workflows/rust-lint.yml` as the first step before `cargo +nightly fmt --all -- --check`. Added `README.md`, `CHANGELOG.md`, and `scripts/ci/check_version_consistency.sh` to the workflow's `paths:` triggers so doc-only or script-only changes also exercise the gate.

## 2. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 2.1 Update or create documentation covering the implementation — new `docs/development/RELEASE_PROCESS.md` (canonical bump procedure), new `docs/COMPATIBILITY_MATRIX.md` (live cross-reference), per-SDK compat callouts, in-place CHANGELOG note documenting the typo + the branch-name retention rationale.
- [x] 2.2 Write tests covering the new behavior — `scripts/ci/check_version_consistency.sh` is the executable test; runs locally and on every push / PR via `.github/workflows/rust-lint.yml`.
- [x] 2.3 Run tests and confirm they pass — `bash scripts/ci/check_version_consistency.sh` reports `OK: all surfaces report 1.15.0`. `cargo +nightly clippy -p nexus-core --all-targets --all-features -- -D warnings` and `cargo +nightly fmt --all -- --check` both clean.
