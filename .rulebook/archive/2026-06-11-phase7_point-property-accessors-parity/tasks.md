## 1. Implementation
- [x] 1.1 Implement point property accessors in the expression evaluator: cartesian x/y/z + crs ("cartesian"/"cartesian-3d"), WGS-84 longitude/latitude/height + crs ("wgs-84"/"wgs-84-3d") — ROOT CAUSE was bigger than points: query-initial `WITH` returned ZERO rows for ANY constant projection (`execute_with` lacked the unit-row seed `execute_project` already had for standalone RETURN). Seed added (gated: no bound variables + every item evaluable without variables, so empty MATCH stays empty). With rows flowing, x/y/z/crs worked via the existing JSON shape; added the missing `height` → `z` accessor alias for WGS-84 3D points (joining latitude/longitude).
- [x] 1.2 Support 3D point construction: point({x, y, z}) and point({longitude, latitude, height}) — already supported by `parse_point_literal`; verified end-to-end with the new tests (CRS names cartesian-3d / wgs-84-3d included)
- [x] 1.3 Accept the Neo4j positional argument form for point.withinBBox(point, bottomLeft, topRight) — added (3-arg positional points alongside the legacy `{bottomLeft, topRight}` map form). BONUS FIX surfaced by suite test 18.04: `Point`'s `Display` emitted Debug crs (`crs: Cartesian`, unquoted), corrupting the planner's stringify-and-reparse filter path (the re-parser consumed `C` as the quote char) — now emits round-trippable Cypher with the quoted canonical crs name.

## 2. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 2.1 Update or create documentation covering the implementation — CHANGELOG [2.3.3] Fixed entry
- [x] 2.2 Write tests covering the new behavior (the 10 section-18 compatibility-suite queries as unit tests) — `point_property_accessors_match_neo4j`: unit-row WITH, 3D cartesian + WGS-84 accessors, all four CRS names, positional + map withinBBox (inside/outside), WHERE-filter withinBBox over a stored property (18.04), and the empty-MATCH-stays-empty guard
- [x] 2.3 Run tests and confirm they pass (compatibility suite 325/325) — **Neo4j compatibility suite: 310 passed / 0 failed (15 intentionally not-run by the suite itself; was 300 passed / 10 failed)**; full lib 2389/2389; workspace 4717/0; clippy 0 warnings; fmt applied
