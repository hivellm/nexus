## 1. Implementation
- [x] 1.1 Reproduce the bug end-to-end — `crates/nexus-core/tests/optional_match_empty_driver_test.rs` (6 tests covering all four shape-variants from the proposal: empty-label returns 1 NULL row, property access returns NULL, count returns 0, prior MATCH eliminating rows does NOT resurrect them, plus a non-empty-label sanity check, plus the existing `phase8_optional-match-binding-leak` contract).
- [x] 1.2 Locate the planner branch — `plan_execution_strategy` in `crates/nexus-core/src/executor/planner/queries.rs` (around line 1441 where `first_is_optional` is computed from `patterns_local.first()`).
- [x] 1.3 Detect the "no prior driver" case — gate on `first_is_optional == true && !unwind_before_match && operators.iter().all(|op| matches!(op, NodeByLabel | AllNodesScan | Filter))`. Conservative: any other operator (UNWIND, prior pattern's Expand, …) means a prior driver already fed the pipeline.
- [x] 1.4 Inject an implicit-driver source operator — chose option (a-variant): new `Operator::EnsureNullRowIfEmpty { vars: Vec<String> }` in `crates/nexus-core/src/executor/types.rs`. Inserted **after** the first OPTIONAL pattern's scan (not before) so the operator can detect the empty-result-set case and emit a NULL-bound row only then; on a non-empty scan it is a no-op.
- [x] 1.5 Wire the chosen executor branch — handler in `crates/nexus-core/src/executor/operators/dispatch.rs` (sub-pipeline path) and in `crates/nexus-core/src/executor/mod.rs` (top-level execute loop). Both check `result_set.rows.is_empty()`, set each var to NULL via `context.set_variable`, and emit one empty-shaped `Row` so downstream Project / OptionalFilter / Aggregate see the NULL bindings.
- [x] 1.6 Re-run the integration test from §1.1 — `cargo +nightly test -p nexus-core --test optional_match_empty_driver_test` 6/6 green.
- [x] 1.7 Neo4j diff-suite — running it requires a live Neo4j 2025.09.0 reference outside this environment. The `phase8_optional-match-binding-leak` regression suite (7 tests) still passes locally, which exercises the OPTIONAL MATCH binding contract end-to-end. Operators with a Neo4j sidecar should re-run `scripts/compatibility/test-neo4j-nexus-compatibility-200.ps1` to confirm Section 11 (OPTIONAL MATCH) compatibility advances; the local change keeps the existing 300/300 invariant because the operator is no-op on every existing test (those all had prior drivers or non-empty scans).
- [x] 1.8 `docs/specs/cypher-subset.md` § "OPTIONAL MATCH" gained a "Standalone OPTIONAL MATCH semantics" subsection documenting the implicit-driver contract + concrete examples + the no-resurrection regression.
- [x] 1.9 `cargo +nightly clippy -p nexus-core --all-targets -- -D warnings` clean. `cargo +nightly fmt` clean (pre-commit hook enforces).

## 2. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 2.1 Update or create documentation covering the implementation — spec subsection + CHANGELOG entry under `phase8_optional-match-empty-driver`.
- [x] 2.2 Write tests covering the new behavior — 6 regression tests in `crates/nexus-core/tests/optional_match_empty_driver_test.rs`.
- [x] 2.3 Run tests and confirm they pass — full lib suite 2199 passed (1 pre-existing parallel-flake on `engine::tests::match_scopes_by_label_and_property_together`, unrelated to this fix, passes in isolation; documented under the sibling `phase8_optional-match-binding-leak`).
