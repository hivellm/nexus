## 1. Implementation

- [x] 1.1 Ran `scripts/compatibility/test-neo4j-nexus-compatibility-200.ps1` against live Neo4j 2025.09.0 community + release Nexus (build `a2c1c5b6`) on 2026-04-19. Result: **300 / 300 passed / 0 failed**. The raw runner banner reads `Total Tests: 300 / Passed: 300 / Failed: 0 / Pass Rate: 100%`. Archive reference: `phase3_unwind-where-neo4j-parity` (commit `aeb83038`) — that task closed the last outlier (14.05 UNWIND with WHERE), so the number is both the current state *and* the historical jump from 299→300.
- [x] 1.2 Ran `cargo +nightly test --workspace` on a freshly-built workspace. Parsed every `test result: ok` line from the output: **2310 passed / 67 ignored / 0 failed** across all crates (nexus-core 1442 lib tests + parser/executor/integration crates + nexus-server + nexus-protocol + nexus-cli + SDK Rust crate + benches compiled-but-not-run). The 67 ignored come from storage-mock / KNN-seed / env-gated tests that need specific hardware (e.g. `#[ignore = "requires SIMD AVX-512"]`) and legitimately don't run in a generic workspace pass.
- [x] 1.3 Three files updated to the real numbers with one canonical status block each:
  - `AGENTS.override.md` header: `v0.12.0 / 293/300 (97.99%) / 2025-12-01` → `v1.0.0 (workspace) / 300/300 / 2026-04-19` with the production-readiness claim intentionally dropped from the header (the explicit criterion block further down in the document is the maintainer-anchored gate; no hand-wave "production ready" in the status field). The "Key Characteristics" block dropped the stale `2949+ tests passing, 70%+ coverage` line in favor of a commit-cite-worthy `2310 workspace tests passing on cargo +nightly test --workspace (0 failed, 67 ignored); 300/300 on the Neo4j diff suite` with the reproduction commands inline.
  - `README.md` "Highlights (v1.0.0)": `299/300 Neo4j compatibility tests pass` → `300/300` with a link to `docs/compatibility/NEO4J_COMPATIBILITY_REPORT.md` citing the 2026-04-19 Neo4j 2025.09.0 diff. Documentation link table footer (line 387) updated from `299/300` to `300/300`. Roadmap's `1.0.0 — current` bullet rewritten from the stale `3361+ unit tests, 299/300 Neo4j compatibility` to `2310 workspace tests passing ... 0 failed, 67 ignored; 300/300 Neo4j diff suite`.
  - `docs/compatibility/NEO4J_COMPATIBILITY_REPORT.md` header: `Version 0.12.0 / 2025-12-01 / 100% compatibility achieved - production ready!` → `Version 1.0.0 (workspace) / Last verified 2026-04-19 against Neo4j 2025.09.0 community / 300/300`. The `Core Cargo Tests: 2949+` line was the canonical lie; replaced with a two-row reproduction table (`Neo4j diff suite 300/300`, `Nexus workspace 2310 passed / 67 ignored / 0 failed`) and inline instructions for the prerequisite Neo4j docker container + the exact `scripts/compatibility/...` invocation so any caller can reproduce the claim.
- [x] 1.4 `docs/ROADMAP.md` header opens with a new block-quote `Current state (2026-04-19)` summarising phases 0-2 shipped in v1.0.0 + pointing at `CHANGELOG.md` for release-by-release detail so the roadmap stays scope-focused without duplicating history. The misleading `## Phase 0: Foundation (Current)` heading changed to `## Phase 0: Foundation ✅ COMPLETED` — Phase 0 is obviously done given Phase 2 already shows `✅ COMPLETED` downstream. No individual phase dates are asserted (the roadmap never carried absolute dates — only `Week N` offsets, deliberately), so the CHANGELOG `##` version-header dates remain the single source of truth for *when* each slice landed.
- [x] 1.5 Swept the whole tree with `grep -rn "Production Ready|100% compatible|2949+|299/300" --include="*.md"` excluding `.rulebook/`, `target/`, `node_modules/`. Per-hit decisions:
  - **CHANGELOG.md** historical entries (`299/300`, `293/300`, `100% Neo4j Compatibility Achieved - 300/300`): all historically-accurate for their date; kept verbatim because CHANGELOG is an append-only history, not a status doc. Rewriting historical entries would falsify the record.
  - **docs/performance/BENCHMARK_NEXUS_VS_NEO4J.md** per-feature `(100% compatible)` bullets (Mathematical functions, List operations, NULL handling, Type conversions, UNWIND operations, MERGE operations): all narrow per-section claims that match the 300/300 total — each section in the diff suite is 100 % pass within its own scope. Kept; the containing doc already cross-references the compat report.
  - **docs/sdks/TEST_COVERAGE_REPORT.md** `### ✅ Production Ready` header: a per-SDK recommendation section (Python / TypeScript / Rust / C# / n8n) with explicit caveats nearby (Go is listed under `### ⚠️ Usable with Caveats`). The phrase is actionable and scoped; kept.
  - **sdks/typescript/node_modules/lilconfig/readme.md** — third-party vendored file, out of scope.

## 2. Tail (mandatory — enforced by rulebook v5.3.0)

- [x] 2.1 Update or create documentation covering the implementation — every rewritten claim now carries a pointer to the run that produced the number: README / compat-report cite commit `aeb83038` (the archive-commit of the `phase3_unwind-where-neo4j-parity` task that lifted the suite to 300/300) + a 2026-04-19 diff date + the Neo4j version (2025.09.0). The compat report also carries the exact `docker run` command for the Neo4j instance + the PowerShell runner invocation so any reader can reproduce the number locally.
- [x] 2.2 Write tests covering the new behavior — `.github/workflows/neo4j-compat-weekly.yml` (new): runs the 300-test Neo4j diff suite every Monday 04:00 UTC against a Neo4j 2025.09.0 community service container + a freshly-built release Nexus. The workflow parses the runner's final banner and fails the job if the pass rate drifts off 300/300, uploads the raw runner log as an artifact for post-mortem, and supports `workflow_dispatch` for on-demand verification. This is the regression guard that keeps §1.3's claim honest — if Nexus or Neo4j ever diverges, Monday's run goes red before the next caller cites the stale number.
- [x] 2.3 Run tests and confirm they pass — `cargo +nightly doc --workspace --no-deps` runs cleanly; three pre-existing rustdoc warnings remain (`nexus-cli` unresolved link to `RpcTransport::connect`; `nexus-server` private-item link in admin module; `nexus-server` unclosed HTML tag `<Str>` in a `//!` comment) — none introduced by this task (doc edits landed in top-of-tree `.md` files, not in `//!` module comments). `cargo +nightly test --workspace` green (2310 passed, 0 failed, 67 ignored — the number cited in the updated docs). `cargo +nightly fmt --all -- --check` + `cargo +nightly clippy --workspace --all-targets -- -D warnings` clean from the prior task's gates; this task touched Markdown / YAML only, no Rust source.
