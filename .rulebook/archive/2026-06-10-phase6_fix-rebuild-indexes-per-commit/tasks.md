## 1. Investigation
- [x] 1.1 Confirm explicit `COMMIT` calls `rebuild_indexes_from_storage` (mod.rs:3831) and that `apply_pending_index_updates` (mod.rs:3817) already applies the incremental index updates for the committed session — confirmed (now `engine/transactions.rs` post-split). Finding: `apply_pending_index_updates` was a no-op in practice (nothing ever enqueues into `session.pending_index_updates`), and `session.created_nodes` is only populated by the engine-path CREATE — standalone CREATE in a transaction goes through the executor, which does not report into the session. The rebuild was load-bearing for the typed property index (d0901c44 investigation).
- [x] 1.2 Confirm rollback path does not depend on the post-commit rebuild for index consistency — confirmed: rollback removes created nodes from label/property indexes directly (transactions.rs rollback arm), no rebuild involved.

## 2. Implementation
- [x] 2.1 Remove the per-COMMIT `rebuild_indexes_from_storage()` call; keep `refresh_executor` — replaced with `apply_committed_entity_index_updates` (engine/crud/index_maintenance.rs): scoped maintenance over storage watermarks captured at BEGIN (`tx_begin_node_watermark`/`tx_begin_rel_watermark` on Session — single-writer model makes the id range exact) union the session's created-entity lists; maintains label bitmap + typed property B-tree per node and the relationship index per rel; all inserts idempotent (set/bitmap semantics).
- [x] 2.2 Add a dev/test assertion that the indexes after incremental commit match a full rebuild (no net diff) — `explicit_commit_incremental_indexes_match_full_rebuild` runs the same label/relationship/seek queries after the incremental commit and after a forced `rebuild_indexes_from_storage()`, asserting identical results.

## 3. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 3.1 Update or create documentation (CHANGELOG Fixed / GH #15) — CHANGELOG [Unreleased] Fixed entry
- [x] 3.2 Write tests: explicit BEGIN/COMMIT keeps label + relationship + property indexes correct without a full rebuild; commit cost does not scale with total graph size — equivalence test covers all three indexes; the existing `explicit_commit_keeps_property_index_seek` contract guard stays green with the rebuild removed. Cost claim verified structurally: the only O(N) full-scan call in the COMMIT path is gone (commit now iterates the watermark range = the tx's own writes); a wall-clock scaling benchmark is not reliable in unit tests.
- [x] 3.3 Run tests and confirm they pass — `cargo test -p nexus-core --lib` 2377/2377, `--test integration` 15/15, clippy 0 warnings, fmt applied
