## 1. Implementation
- [x] 1.1 Read `phase6_opencypher-subquery-transactions` and confirmed the executor side **already shipped** in slice-2 (`IN TRANSACTIONS` batching + `ON ERROR` recovery + `REPORT STATUS`) and slice-3 (`IN CONCURRENT TRANSACTIONS` + atomic per-batch rollback via `CompensatingUndoBuffer`). The phase7 task proposal was authored before the parent's slice-2/3 work was audited; the items below are checked because each is already implemented and tested in `crates/nexus-core/src/executor/operators/call_subquery.rs` (715 LOC, fully wired).
- [x] 1.2 `Operator::CallSubquery` carries `in_transactions: bool`, `batch_size: Option<usize>`, `concurrency: Option<usize>`, `on_error: OnErrorPolicy`, `status_var: Option<String>`. Driver `execute_call_subquery` opens a fresh transaction per batch via `run_call_subquery_in_transactions` (line 214) at `crates/nexus-core/src/executor/operators/call_subquery.rs:177`.
- [x] 1.3 Chunking with default `DEFAULT_BATCH_SIZE = 1000` (line 60) and explicit `OF N ROWS` override. Outer rows are batched before driving the inner subquery; each batch consumes `min(batch_size, remaining)` rows.
- [x] 1.4 Commit-between-batches: each batch installs a per-attempt `CompensatingUndoBuffer` (`Arc<Mutex<Vec<CompensatingUndoOp>>>`); on success the buffer is discarded and writes stay committed for the next batch's visibility. On failure the buffer is drained in reverse to restore catalog state.
- [x] 1.5 `OnErrorPolicy::Continue` collects errors, marks the batch's `committed=false` and `err` field, advances to the next batch (line 346 retry-log path).
- [x] 1.6 `OnErrorPolicy::Break` halts on first failure with partial results preserved (status rows already emitted survive).
- [x] 1.7 `OnErrorPolicy::Fail` (default) aborts the whole call; the batch's compensating buffer rolls back its writes before the error propagates.
- [x] 1.8 `REPORT STATUS AS s` emits one MAP-typed row per batch under the declared name with keys `started`, `committed`, `rowsProcessed`, `err`. Status keys diverge slightly from the proposal's `{committed, transactionId, errorMessage, started, finished}` — Nexus uses `rowsProcessed` instead of `transactionId` (transaction IDs are MVCC-internal and not user-meaningful here) and `err` instead of `errorMessage`. This is what the parent task spec specified and what the existing tests pin (`call_subquery_in_transactions_report_status_emits_one_row_per_batch`).
- [x] 1.9 Nested `CALL { … } IN TRANSACTIONS` rejected via `ERR_CALL_IN_TX_INVALID_STATE` and friends at lines 95, 108, 115. Concurrent-pool requires `IN TRANSACTIONS`; status-var requires `IN TRANSACTIONS`; `OnErrorPolicy != Fail` requires `IN TRANSACTIONS` — all enforced.
- [x] 1.10 Executor tests: `crates/nexus-core/tests/call_subquery_test.rs` ships **20 passing tests**, including 9 dedicated to the IN TRANSACTIONS surface (`call_subquery_in_transactions_runs_unwind_create_to_completion`, `call_subquery_in_transactions_report_status_emits_one_row_per_batch`, `call_subquery_in_transactions_rolls_back_partial_writes_on_failure`, `call_subquery_in_transactions_default_fail_propagates_inner_error`, `call_subquery_in_transactions_continue_records_failed_batches`, `call_subquery_in_transactions_break_stops_on_first_failure`, `call_subquery_in_transactions_retry_escalates_to_fail_when_exhausted`, `call_subquery_in_transactions_on_error_combinations_are_accepted`, `call_in_concurrent_transactions_requires_in_transactions`). Verified green via `cargo +nightly test -p nexus-core --test call_subquery_test` → `20 passed; 0 failed`.
- [x] 1.11 Neo4j diff-suite stays at 300/300 — the IN TRANSACTIONS feature was already credited in the parent task's archival capture and the static query strings under `scripts/compatibility/test-neo4j-nexus-compatibility-200.ps1` were not modified by this task.
- [x] 1.12 `docs/specs/cypher-subset.md` already documents the surface as supported (lines 755–774): the `CALL { } IN TRANSACTIONS OF N ROWS [REPORT STATUS AS var] [ON ERROR CONTINUE|BREAK|FAIL|RETRY n]` shape with the exact `OnErrorPolicy` semantics. No spec change needed.

## 2. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 2.1 Update or create documentation covering the implementation — the parent task's CHANGELOG entries already document the feature; this task adds an audit-trail line under `[1.15.0]` `### Discovered (no code change) — phase7_call-in-transactions-executor` confirming the work is already shipped and pointing at the existing test + spec coverage.
- [x] 2.2 Write tests covering the new behavior — the existing 20 tests in `crates/nexus-core/tests/call_subquery_test.rs` (9 IN-TRANSACTIONS-specific) ARE the test surface; no new tests required because the implementation is complete.
- [x] 2.3 Run tests and confirm they pass — `cargo +nightly test -p nexus-core --test call_subquery_test` reports `20 passed; 0 failed; 0 ignored`. `cargo +nightly clippy -p nexus-core --all-targets --all-features -- -D warnings` clean. `cargo +nightly fmt --all -- --check` clean.
