## 1. Implementation
- [x] 1.1 Enforce inline target-node labels in required expand — root cause: planner skipped emitting NodeByLabel/Filter for post-relationship pattern nodes assuming Expand enforced, but execute_expand never checked. Operator::Expand gains target_labels (types.rs); planner threads each hop's inline labels (relationships.rs, incl. prev_node_labels pairing so the OPTIONAL reversed-traversal swap keeps the predicate with the node occupying target_var); enforcement via node_value_has_all_labels (AND-intersection on _nexus_labels, fails closed, no extra store round-trip); dynamic $-sentinels resolved at exec time per the existing Filter convention (missing param errors); undirected hops check the actual per-candidate other-endpoint. Multi-hop chains enforced per Expand operator
- [x] 1.2 Optional expand label rejection pads — label-rejected candidate counts as no match, feeding matched_for_this_source → NULL-padded row via the shared pad_optional_row helper; pre-bound targets preserved. A/B TCK measurement (revert/reapply): +11 net scenarios, 0 newly broken — useCases/triadicSelection 15→19 (19/19 complete) + 7 collateral gains in clauses/match [2][6][28], clauses/create [3], clauses/with [1][3], clauses/with-orderBy [15]. Independent review APPROVE (reversed pairing, undirected, sentinels, multi-label all traced; collateral gain validated against Match3.feature; required-expand phantom-row issue untouched)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — CHANGELOG entry; OPENCYPHER_TCK_REPORT.md regenerated by the harness run
- [x] 2.2 Write tests covering the new behavior — 9 regression tests in tests/executor/expand_target_label_enforcement_test.rs: both probe queries, positive optional control, 3-hop chain (every hop independently enforced), undirected variant, multi-label AND-intersection, reversed-pairing shape end-to-end (both null-pad and bind directions), dynamic $-sentinel via Expand incl. missing-param error
- [x] 2.3 Run tests and confirm they pass — executor group 9/9 new + suite green; cypher 499/0; regression 227/0; compatibility 245/0; clippy -D warnings clean; rustfmt clean; TCK A/B verified +11/0 broken
