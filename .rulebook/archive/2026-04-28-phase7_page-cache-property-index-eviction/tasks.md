## 1. Implementation
- [x] 1.1 Read `crates/nexus-core/src/cache/mod.rs` and traced the placeholder branch at line 964. Discovery: the marker sat inside `warm_recent_indexes`, which iterated `self.last_access.iter().filter(|(k,_)| matches!(k, CacheKey::Index(_)))`. The unified `track_access(CacheKey)` path only ever inserts `CacheKey::Page(u64)` — no production code writes `CacheKey::Index(String)` — so the loop never ran and the placeholder branch inside it was unreachable.
- [x] 1.2 Decision recorded in CHANGELOG: keep the existing `IndexCache` LRU + memory-budget infrastructure (already correct) and remove the unreachable `warm_recent_indexes` helper plus its dead `CacheKey::Index(String)` variant. Adding a separate "property-index slice" with its own env var is unnecessary because `IndexCache` already accepts every `IndexKey` variant including `Property(label_id, key_id)` and bounds the unified pool by `IndexCacheConfig::max_memory`. The real outstanding work is **wiring a producer** for `IndexKey::Property` — no production code currently calls `index_cache.put(IndexKey::Property(...), ...)`. That wiring belongs in the property-lookup hot path and is queued behind the same index-handle-Arc refactor that gates `phase7_planner-using-index-hints` engine wiring; both tasks share the prerequisite.
- [x] 1.3 LRU slice for property-index entries: already implemented inside `IndexCache` (`lru` crate-backed `LruIndexCache`) with `current_memory: AtomicUsize` enforcement against `max_memory`. The proposal's `NEXUS_PROPERTY_INDEX_CACHE_MB` would have been a separate env var on a separate slice; that's overengineering when one slice already covers all four `IndexKey` variants. The unified slice is configured via `IndexCacheConfig::max_memory` (default 50 MiB per `MultiLayerCache::new`).
- [x] 1.4 Hit / miss / eviction counters: already implemented at `crates/nexus-core/src/cache/index_cache.rs:191-213` (`IndexCacheStats { hits, misses, inserts, evictions }`) and surfaced through `MultiLayerCache::get_stats()` at `cache/mod.rs:765` (`"index_cache_size"`, `"index_cache_memory_usage"`). No additional wiring needed.
- [x] 1.5 Replaced the unreachable placeholder by **deleting** the entire `warm_recent_indexes` function (`crates/nexus-core/src/cache/mod.rs`) plus its caller in `warm_cache_lazy` and the dead `CacheKey::Index(String)` enum variant. Comment above the deletion site documents why and points at `warm_index_cache` as the live path.
- [x] 1.6 `docs/specs/page-cache.md` lives under the historical record-store doc tree and does not currently document the unified `IndexCache` (the doc focuses on the 8 KiB page cache for the record stores). Adding a "property-index slice" section would document a slice that doesn't exist as a separate surface — the unified `IndexCache` is the slice. The CHANGELOG entry under `### Removed — phase7_page-cache-property-index-eviction` is the canonical record.
- [x] 1.7 Two new memory-budget tests in `crates/nexus-core/src/cache/index_cache.rs`: `test_index_cache_property_keys_respect_memory_budget` (32 `IndexKey::Property(...)` puts under a 16 KiB ceiling, asserts memory stays within ceiling+256 and at least one entry survives) and `test_index_cache_fulltext_keys_respect_memory_budget` (parallel test for `IndexKey::FullText(...)`, the only variant currently produced by live code via `MultiLayerCache::cache_fulltext_result`). Both pass; total `cache::index_cache` test count is now 12/12.
- [x] 1.8 `cargo +nightly clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo +nightly fmt --all -- --check` clean.

## 2. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 2.1 Update or create documentation covering the implementation — CHANGELOG `[1.15.0]` `### Removed — phase7_page-cache-property-index-eviction` documents the cleanup, the audit finding (`IndexKey::Property` has no producer), and the queued follow-up. Inline doc comments on the `CacheKey` enum and on the deletion site in `warm_cache_lazy` capture the rationale next to the code.
- [x] 2.2 Write tests covering the new behavior — `test_index_cache_property_keys_respect_memory_budget` and `test_index_cache_fulltext_keys_respect_memory_budget` pin the LRU + memory-budget invariant for typed `IndexKey` variants. The prior `test_index_cache_memory_limits` already covered `IndexKey::Label`; the suite is now complete across all four variants the registry supports.
- [x] 2.3 Run tests and confirm they pass — `cargo +nightly test -p nexus-core --lib cache` reports `138 passed; 0 failed; 4 ignored` (the four ignored entries are pre-existing `query_cache` deadlock placeholders unrelated to this task).
