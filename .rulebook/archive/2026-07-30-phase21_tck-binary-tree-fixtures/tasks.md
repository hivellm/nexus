## 1. Implementation
- [x] 1.1 binary-tree-N fixture step — CREATE scripts reproduced verbatim from upstream `tck/graphs/binary-tree-{1,2}/binary-tree-{1,2}.cypher` at the pinned commit (`tck_common::{BINARY_TREE_1_CYPHER,BINARY_TREE_2_CYPHER,binary_tree_cypher}`); new `Given the binary-tree-(1|2) graph` step in tck_opencypher.rs seeds a fresh isolated engine, same as `having executed:`; skip-list entry removed
- [x] 1.2 triadic selection support — two root causes fixed in the executor: (1) WITH-attached WHERE now evaluates inside execute_with against the merged pre-projection scope (predicate threaded into Operator::With; the old post-With Filter lowering could never see non-projected vars like `r`); (2) execute_expand's OPTIONAL padding now also fires when candidates exist but all are rejected for the bound target (pad_optional_row helper shared by all three padding sites; pre-bound targets preserved, never nulled). useCases/triadicSelection 2/19 → 15/19; overall TCK 1162/3868 (30.0%). The remaining 4 scenarios ([7],[8],[9],[17]) fail on a distinct pre-existing defect — inline label constraints not enforced on expand target nodes — tracked as its own task `phase21_tck-expand-target-label-enforcement` (a second surfaced defect, comma-pattern binding materialization, is tracked as `phase21_tck-comma-pattern-binding-materialization`). Independent opus review: APPROVE; 7 regression tests in tests/cypher/with_where_scoping_tests.rs

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation (OPENCYPHER_TCK_REPORT.md regenerated; VENDOR.md provenance already covers the corpus, fixture provenance documented inline in tck_common.rs)
- [x] 2.2 Write tests covering the new behavior (tests/tck_fixtures.rs: node/rel/label counts for both fixtures + `binary_tree_cypher` resolution)
- [x] 2.3 Run tests and confirm they pass (tck_fixtures.rs 3/3 pass; clippy/fmt clean; TCK skip-list 90 -> 71 (-19 binary-tree entries removed), useCases/triadicSelection 0/0/19 -> 2/17/0)
