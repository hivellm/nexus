# Rotated session history

### 2026-07-21
Completed and archived phase14_fix-external-id-write-path (19/19). Maintainer decisions: 2.5 per-row UNWIND _id stays a parse error; 2.6 rel-MERGE endpoints gained _id support. Implementation: parser extract_underscore_id_from_pattern gained allow_per_node (MERGE per-endpoint _id via the previously-unpopulated NodePattern.external_id_expr; CREATE stays strict), merge_single_node now resolves per-node _id with index-before-pattern-search and create_node_with_external_id(Match). 9 new tests (cypher_external_id_rel_merge.rs), cypher suite 348/348. 3.4: new live-e2e CI job in sdk-rust-test.yml (14/14 locally). 4.1-4.3: closed not-applicable — workspace sweep found no consumer ever wrote _id through broken forms. Docs: cypher-subset.md § Write Forms Honouring External ID, CHANGELOG, EXTERNAL_IDS.md (first docs pass wrongly deleted valid ON CONFLICT syntax and added a parse-error example — caught in review, corrected). Full workspace gate green. Reported on issue #29 with regression-framing correction (Cortex writes plain id, never _id; not a 2.3.4 regression). Commits: dd562189 (fix), 8a5e0881 (archive). Next: remaining backlog tasks (phase0 fixes, phase7, phase9 in-progress store-lock-read-concurrency at 5/14).

### 2026-07-22
Sprint resumed after the disk-leak interrupt. Completed + archived phase0_fix-where-predicate-reparse-precedence (CRITICAL). Commit e9280c9b (fix) + b1ec1c90 (archive). WHERE clauses were lowered to a plain string (Operator::Filter/OptionalFilter) and re-parsed at exec time, but expression_to_string emitted BinaryOp/UnaryOp WITHOUT parens, so `(n.a OR n.b) AND n.c` round-tripped to `n.a OR n.b AND n.c` and re-parsed as `n.a OR (n.b AND n.c)` — silent boolean flip; same for `(n.x-n.y)*n.z`. DECISION (task §2.3): took the MINIMAL faithful-serialization fix (it IS the root cause of the precedence bug), NOT the structural AST-carry — split expression_to_string into expr_to_string_impl(expr, parenthesize) with a render_operand closure wrapping every compound child in parens (provably round-trip-correct, no precedence table); flag propagates through all nested positions. Added predicate_to_string (parenthesize=true) used ONLY by the 5 live WHERE lowering sites (strategy.rs:425/1051, planner_core.rs:759/825/1197 — implementer found the 5th, :759, via exhaustive grep of Filter constructions); kept expression_to_string (false) for the ~20 display/alias/ORDER-BY callers. dispatch.rs:1373/1405 confirmed DEAD (ast_to_operators, zero callers, + it's a DIFFERENT Executor::expression_to_string in create.rs). The CASE/comprehension "?" hazard genuinely can't be fixed at the string layer -> filed phase0_fix-where-predicate-case-comprehension-lost (structural AST-carry). Tests: where_predicate_precedence_test.rs (5: bool flip, arith flip, OPTIONAL MATCH WHERE/OptionalFilter, NOT-nesting, projection control). Verified independently: 5/5 discriminating (3 fail pre-fix, 2 control pass), executor 190/190, full workspace CARGO_EXIT=0 5092 passed/0 failed, clippy+fmt clean, pre-commit hooks green. CHANGELOG updated. NEXT in EXECUTOR/PLANNER serial cluster: optional-match-var-scoping -> where-clause-index-seek -> skip-dropped[groundwork f95d4458] -> relationship-write-clauses-dropped -> multi-hop-count-star -> cypher-unbounded-alloc-oom. Also queued: STORAGE x5, WAL x2, and follow-ups (loadcsv, shortestpath, where-case-comprehension, engine-temp-dir-catalog-lock-residual). This session's landed fixes: plan-reorder-drops-predicates 3b13f530, varlength-multi-reltype-dropped c76e41c5, tempdir-record-store-leak a3c10a49, where-predicate-reparse-precedence e9280c9b.

### 2026-07-22
URGENT user-reported disk leak — FIXED + archived phase0_fix-tempdir-record-store-leak. Commit a3c10a49 (fix) + 8ae647f1 (archive). User's C: had 21,000+ leaked %TEMP%\.tmpXXXXXX dirs (~100 GB) full of record-store files. ROOT CAUSE (root-caused, not guessed): Executor::default() (executor/dispatch.rs) did tempfile::tempdir().keep() — deliberately disarming removal — leaking one store dir per call; used by 50+ test files → hundreds per `cargo test` run. Engine::new() same pattern. Stores also had no Drop. TestContext is NOT the culprit (isolated nexus-test-tmp base + deferred sweep — correct). FIX: RecordStore gained _cleanup: Option<Arc<TempDirGuard>> as the LAST field (drops after mmaps) + RecordStore::new_temporary() (nexus-store- prefix) attaching the guard; dir removed exactly when the last store clone drops — cross-platform, no timer/sweep (server-safe). Executor::default() + Engine::new() use new_temporary(), .keep() gone; Engine dropped its _temp_dir field and shares a bootstrap_with_storage helper (avoids a double-open that would delete the dir mid-init). Persistent stores keep _cleanup=None. Regression tests: dir removed on drop; with a clone, removed only on last-clone drop. EMPIRICAL PROOF: ran 437 tests (engine/storage/executor) that used to leak hundreds → 0 net dirs leaked. Full workspace gate 5087 passed/0 failed (CARGO_EXIT=0); clippy+fmt clean; pre-commit hooks green. Cleaned up all 21k+1430 existing leaked dirs (freed disk, 206G free). CHANGELOG updated; memory saved (project-tempdir-record-store-keep-leak). Filed follow-up phase0_fix-engine-temp-dir-catalog-lock-residual (~2/437 Engine::new() dirs survive on Windows when LMDB catalog still locked as guard fires — minor tail). NOTE: the implementer agent falsely reported "done" with ZERO changes on disk the first time, and later falsely claimed "full workspace green" when an isolated re-run was needed to confirm — ALWAYS verify via git + independent re-run + real leak-count, never trust the agent's summary. Also: a background `cargo test --workspace` "exit 1" was a fluke of a missing log path / concurrent cleanup, not a real failure — the clean isolated re-run was green. CONTEXT: this interrupted the phase0 planner-fix sprint (user pivoted mid-way); phase0_fix-where-predicate-reparse-precedence was reset to pending (no code had landed) with its case/comprehension follow-up filed. Sprint fixes landed earlier this session: plan-reorder-drops-predicates (3b13f530), varlength-multi-reltype-dropped (c76e41c5).

### 2026-07-22
Fixer sprint (continuation) — 2nd fix this run. Completed + archived phase0_fix-varlength-multi-reltype-dropped (CRITICAL query correctness). Commit c76e41c5 (fix) + 42740305 (archive). `[:R1|R2*m..n]` variable-length patterns traversed only the FIRST named type; nodes reachable solely via a later type were silently dropped. Root: Operator::VariableLengthPath.type_id was Option<u32> and lowering did type_ids.first().copied(). Fix: retyped to type_ids: Vec<u32> (mirrors Expand), pass full parsed list in relationships.rs, thread through path.rs (VariableLengthPathVisitor.type_filter: Vec<u32>, execute_variable_length_path(&[u32])) + both dispatch arms. Semantics preserved (empty=all, populated=OR — same rule find_relationships uses for Expand). REVIEW KEY FINDING: Operator derives only Debug+Clone (NO serde) -> field rename has zero wire/cache/explain blast radius; also the `use_optimized_traversal` branch is DEAD CODE (hardcoded false) so the load-bearing fix is the manual BFS fallback (updated the dead branch too for hygiene). Tests: variable_length_multi_reltype_test.rs (FOLLOWS-only, mixed multi-hop, unqualified control, 3-way [:A|B|C*1..2] union) + QPP-rewrite parity test in engine/tests/query.rs via the sanctioned set_qpp_legacy_rewrite_enabled toggle (env is read-once into a OnceLock so std::env::set_var is a no-op mid-process) under #[serial_test::serial]. Independent code-review SHIP, 0 blockers, full VariableLengthPath consumer audit. Verified: 5/5 targeted + executor 185/185 + full workspace CARGO_EXIT=0 & FAILED=0 (5083 passed/0 failed), clippy+fmt clean, both pre-commit hooks green. CHANGELOG + cypher-subset.md BNF (Type ::= Identifier ('|' Identifier)*) updated. FILED follow-up phase0_fix-shortestpath-multi-reltype-dropped: find_shortest_path/find_all_shortest_paths/find_paths_dfs (called ONLY by shortestPath()/allShortestPaths() in fn_graph.rs) still cap at first type — reviewer confirmed genuinely separate from execute_variable_length_path. Knowledge entry added (single-hop vs var-length operators must share the Vec<u32> type_ids shape). NOTE: Agent tool subagents (researcher/implementer/reviewer) reliably PAUSE mid-task in this env — must nudge via SendMessage repeatedly until the final report; verify all ground truth via git + independent re-run, never trust the truncated notification result. NEXT in EXECUTOR/PLANNER serial cluster: where-predicate-reparse-precedence -> optional-match-scoping -> where-clause-index-seek -> skip-dropped[groundwork f95d4458] -> relationship-write-clauses-dropped -> multi-hop-count-star -> cypher-unbounded-alloc-oom. Also queued: STORAGE x5, WAL x2, plus the two new follow-ups (loadcsv, shortestpath).

### 2026-07-22
Fixer sprint (continuation). Completed + archived phase0_fix-plan-reorder-drops-predicates (CRITICAL planner correctness). Commit 3b13f530 (fix) + ce6e9dee (archive). The cost-based optimize_operator_order (cost.rs) bucketed variable-BINDING operators into the catch-all `others` bucket, recombined AFTER `filters`; since an unbound var evaluates Null->false, any WHERE predicate on a var bound by such an operator was silently dropped (e.g. `MATCH (a:A)-[:R*1..2]->(b) WHERE b.name='x'` returned 0 rows). KEY FINDING via research: NodeIndexSeek/CompositeBtreeSeek were ALREADY moved to `scans` by prior phase0_fix-correlated-predicate-index-seek work, so the spec's §1.1/§1.2 cases already passed; the remaining live-broken binders were VariableLengthPath, QuantifiedExpand, SpatialSeek. Fix: SpatialSeek->scans (both the unwind_before_scan detection loop and main bucketing match), VariableLengthPath+QuantifiedExpand->expansions; added an INVARIANT comment (no binding operator may fall through to `others`). Full topological reorder (§3.2) evaluated + DEFERRED as out-of-scope on this hot/entangled file — documented trade-off. TDD: implementer caught that the QuantifiedExpand behavioral test initially PASSED without the fix (a SEPARATE pre-existing planner gap: the QPP trailing boundary node `b` also gets an independent AllNodesScan), and redesigned with an adversarial decoy seed to discriminate the real ordering bug. New test file plan_binding_operator_order_test.rs (5 tests: behavioral+plan-order for VLP & QuantifiedExpand + NodeIndexSeek regression lock), registered in tests/executor/main.rs. Independent code-review verdict SHIP (0 blockers) with a full 35-variant Operator completeness audit: Join/HashJoin/IndexScan are dead in the live planner (only via unused optimizer.rs); the ONE live same-class gap is LoadCsv (per-row binder still in `others`, not in the unwind_before_scan loop) -> filed phase0_fix-loadcsv-reorder-drops-predicates with the reviewer's concrete trace. Lower-confidence follow-ups noted (CallProcedure/CallSubquery YIELD bindings; the QPP-boundary AllNodesScan redundancy). Verified independently: 5/5 targeted, executor group 181/181, CARGO_EXIT=0 + `test result: FAILED`=0 across full workspace (5078 passed/0 failed), clippy+fmt clean, both pre-commit hooks green. CHANGELOG updated; cypher-subset.md had no planner-ordering section so no spec change. NEXT in the EXECUTOR/PLANNER serial cluster (shared hot files strategy.rs/cost.rs): varlength-multi-reltype-dropped -> where-predicate-reparse-precedence -> optional-match-scoping -> where-clause-index-seek -> skip-dropped[groundwork f95d4458] -> relationship-write-clauses-dropped -> multi-hop-count-star -> cypher-unbounded-alloc-oom. Also queued: STORAGE x5, WAL x2, and the new loadcsv fix.

### 2026-07-22
2026-07-22 fixer sprint (continuation). Delivered the test-flake fix + all 6 disjoint "Batch 1" backlog fixes, each research-first + TDD + independent review where warranted + docs + committed + archived on release/3.0.0. Commits: (1) de-flake DatabaseClosing 6ce0f40b — root-caused to the process-shared test catalog env being closable mid-run; fixed by PINNING the shared TEST env open for the whole process (PINNED_TEST_ENVS static in catalog/store.rs), killing the flake class across ALL test binaries in ~15 lines instead of converting 500+ call sites (also converted the 19 cypher modules to isolated catalogs as a bonus); 3x workspace proof zero DatabaseClosing; code-review passed. (2) create-ignores-arrow-direction cebe7962 — CREATE/MERGE now honour <-/-> (Incoming swaps src/dst in all 3 CREATE paths + MERGE path); undirected -[:T]- rejected in CREATE (Neo4j parity), kept as outgoing in MERGE (TCK Merge5). (3) deleted-properties-resurrected 7319513a — delete now writes an on-disk tombstone (ENTITY_TYPE_TOMBSTONE=0xFF) the rebuild scanner honours; back-compat reconciles legacy un-tombstoned entries against the record store; review: correct, no blockers. (4) knn-index-divergence 9cd07bfd — re-insert no longer leaks the stale HNSW entry (tombstone-by-unmapping since hnsw_rs 0.3 has no delete API), stats count nodes not slots, knn_evict_node primitive added; scope option (b): write-path wiring is a documented follow-up. (5) fts-async-writer-ordering 5cbc41e6 — apply_batch coalesces per-node to net outcome with needs_delete_first, applies deletes before adds, preserving per-id del->add order (SET/REMOVE refresh no longer wipes fresh content) while keeping cross-id batching. (6) ingest-bulk-path a6661012 — /ingest now honours NodeIngest.id as a correlation key, returns node_ids in input order (relationships compose), goes straight to Engine::create_node/create_relationship under one lock per chunk (kills the ~11x per-row parse/plan cost), best-effort-not-atomic documented; review passed; residuals: SDK caller survey (§1.2) and empirical 5000-node re-benchmark (§3.2, folded into phase7). (7) password-hashing 07646165 — unsalted SHA-512 -> Argon2id (per-password salt, reusing the API-key KDF), constant-time verify, username-enum timing leak closed via verify_dummy_password, O(N)->O(1) API-key verify via embedded UUID (+forged-token defense), transparent SHA-512->Argon2 rehash-on-login; all password sites fixed (auth module + cypher CREATE USER + main.rs bootstrap); security-reviewer verdict: safe to ship, no blockers. Plus 7 rulebook archive commits. Workspace green throughout (per-fix suites + hooks). KEY LEARNING: the Agent tool's isolation:"worktree" is BROKEN in this env — it branches worktrees from a STALE base commit (e12b8590, ~877 files diverged from release/3.0.0), so parallel worktree fixers all worked against unusable old code; abandoned it and ran fixes SEQUENTIALLY in the main tree (one at a time, commit between each). Also: background implementer agents pause mid-task repeatedly — must be resumed via SendMessage until the commit lands; verify ground truth via git, never trust the truncated notification result line. Also: rulebook archive validator requires the canonical tail phrases ("Update or create documentation covering the implementation" / "Write tests covering the new behavior" / "Run tests and confirm they pass") physically present in tasks.md — tailWaiver alone doesn't satisfy it. NEXT: the ~16 remaining phase0 tasks are 3 entangled clusters that MUST be done serially (shared hot files): STORAGE x5 (record_store_ops.rs — order: store-size-per-clone -> storage-oob-panics -> relationship-publish-ordering -> deleted-props[done] -> perf-reverse-adjacency), WAL x2 (writer.rs — torn-tail-recovery BEFORE durability-gaps), EXECUTOR/PLANNER x9 (strategy.rs/cost.rs — CRITICAL: plan-reorder-drops-predicates -> varlength-multi-reltype -> where-predicate-reparse-precedence, then optional-match-scoping, where-clause-index-seek, skip-dropped[groundwork in f95d4458], relationship-write-clauses-dropped, multi-hop-count-star, cypher-unbounded-alloc-oom). Known separate flakes to watch (NOT DatabaseClosing): fulltext writer_honours_max_batch_capacity_trigger + fulltext_crash_recovery wal_replay (Windows Tantivy file-lock) — pass in isolation.

### 2026-07-22
Fixer sprint completion (2026-07-21/22). Three fix tasks completed and archived: (1) merge-relationship-dropped (a047eade) — variable-less relationship MERGE now creates/matches the whole pattern, node-targeted ON CREATE/ON MATCH SET applies (apply_merge_rel_set replaced by delegation to apply_set_clause); (2) match-create-inline-node-rel-dropped (87ecca67) — Relationship arm of execute_create_with_context now lookahead-creates inline targets before writing the edge, shared create_pattern_node_with_context helper, multi-hop + anonymous anchors work; (3) order-by-on-call-yield (f95d4458) — planner's pattern-less branches now emit Sort -> Skip -> Limit; new Operator::Skip added end-to-end; spec SKIP-context claims verified by direct test. Full benchmark re-sweep completed and phase9 archived (cdd7f5c7): serial 57/57 Lead, concurrency @64w all four scenarios lead Neo4j (merge_singleton 35.8k, count_all 89.8k, two_hop 20.4k, point_read 78k); BENCHMARK_2026.md addendum updated. Four new backlog tasks filed with full proposals: create-ignores-arrow-direction (edges written in array order regardless of <-/->), multi-hop-count-star-incorrect (phantom count), skip-dropped-on-pattern-queries (MATCH...SKIP silently ignored — groundwork landed in f95d4458), de-flake-shared-catalog-cypher-tests (DatabaseClosing class, same as 1acc3bb1's executor fix). Session totals across both days: 6 bugs fixed+archived, 1 perf task archived (33x merge_singleton), 1 flake fixed, 6 new bugs filed, ~18 commits, workspace 5150/5150 green at HEAD 9775e170. Next candidates: fix-skip-dropped-on-pattern-queries (groundwork ready), de-flake task, create-ignores-arrow-direction (behavioral, needs care), or the remaining phase0 backlog.

### 2026-07-21
Second block of the 2026-07-21 session. (1) phase9 write ceiling: refresh_executor skip-on-no-mutation landed (12653c91) — merge_singleton 1.1k→37-39k qps (33x), p99 59ms→2ms, reads neutral; 18 visibility tests; gates 3.2/3.3 closed. (2) DatabaseClosing flake root-caused (query_analysis_test shared default catalog) and fixed (1acc3bb1), unblocking user's push. (3) Serial stragglers: score_gt_half closed as sampling noise (identical compare_values path); db.labels gate re-measure surfaced a P1 — bare CALL queries routed to a detached bootstrap executor with empty catalog. (4) P1 fixed (a7e78078): CallProcedure/CallSubquery now engine clauses (live catalog, registries, database routing, closes CALL-subquery data-loss class) + Catalog::register_property_keys at 12 write chokepoints incl. bulk loader; regression tests fail-without-fix; suites cypher 348 / executor 172 / nexus-server 662 green. Docs (0460a4c8). Post-fix verification: content == Neo4j exactly, latency 25-115x inside the 1.2x gate; task archived (bbc0634d); phase9 4.1 closed. New backlog tasks filed with full proposals: phase0_fix-match-create-inline-node-rel-dropped (MATCH...CREATE inline target node loses relationship), phase0_fix-order-by-on-call-yield (ORDER BY ignored after CALL YIELD); repro evidence added to phase0_fix-merge-relationship-dropped. User feedback memorized: never put task/phase names in code/tests/comments. Remaining in phase9: 4.3 (~100ms periodic outlier on rel creates), 5.1 (full re-sweep with CLEAN data dir — default bench DB polluted with ~2600 nodes), 6.x tail.

### 2026-07-22
Completed + archived phase0_fix-optional-match-var-scoping (HIGH). Commit 877b81df (fix) + 9b27d671 (archive). OPTIONAL MATCH decided nullable vars by positionally skipping the pattern's first node (is_first_node in planner_core.rs), assuming it's the pre-bound anchor. Wrong for reverse-direction (`(b)-[:KNOWS]->(a)` with a bound → nulled the BOUND a, inverted semantics) and standalone (`OPTIONAL MATCH (c:Company)` → empty optional_vars → WHERE lowered as plain Filter → driver rows DROPPED instead of c=NULL, broke LEFT OUTER JOIN). §2.1 FINDING: no existing bound-var accumulator at the site (strategy.rs's previously_bound_vars is a different function/purpose); introduced bound_vars: HashSet<String> across the plan_query clause walk. Extracted collect_pattern_variables (no positional skip), compute optional_vars = pattern_vars - bound_vars, and bound_vars.extend(pattern_vars) after EVERY match (optional or not) so chained OPTIONAL MATCH accumulates scope right. OptionalFilter/execute_optional_filter unchanged. Tests: optional_match_var_scoping_test.rs (5: reverse-dir, standalone, forward baseline, 2 chained). Verified: 5/5 (2 fail pre-fix, 3 pass incl chained as regression-guards), executor 195, cypher 370, regression 141, full workspace CARGO_EXIT=0 5097 passed/0 failed, clippy+fmt clean. CHANGELOG updated (spec unchanged — OPTIONAL MATCH section documents EnsureNullRowIfEmpty not var-scoping); knowledge entry added. NEXT in cluster: where-clause-index-seek -> skip-dropped[groundwork f95d4458] -> relationship-write-clauses-dropped -> multi-hop-count-star -> cypher-unbounded-alloc-oom. SESSION TALLY (5 fixes landed+archived): plan-reorder-drops-predicates 3b13f530, varlength-multi-reltype-dropped c76e41c5, tempdir-record-store-leak a3c10a49 (urgent disk leak), where-predicate-reparse-precedence e9280c9b, optional-match-var-scoping 877b81df. Follow-ups filed: loadcsv-reorder, shortestpath-multi-reltype, where-predicate-case-comprehension, engine-temp-dir-catalog-lock-residual.

### 2026-07-23
Completed + archived phase0_fix-where-clause-index-seek (4 sub-fixes, done in 2 passes). Commits: f8eb857e (#5+#6), 67eba262 (#7+#8), f213daab (archive). #5: WHERE-form `var.prop = <literal>` on an indexed prop now lifts to NodeIndexSeek at plan time (decompose WHERE into AND-conjuncts, lift the matching one, drop from residual Filter; OPTIONAL MATCH skipped; constant literal only — $param/range deferred). #6: unindexed-access notification extended to range/IN/STARTS WITH/CONTAINS (equality-on-indexed now silent). #7: composite-index seek — CompositeBtreeRegistry already existed+wired into executor but CompositeBtreeSeek was NEVER constructed; threaded the registry into QueryPlanner (with_composite_index: struct field in mod.rs, UNION sub-planner copy in planner_core.rs, production wiring in dispatch.rs plan_ast) + composite_index_seek_for (emit only when full key set present as literals, index key order, preferred over single-prop; partial never mis-seeks). #8: Join Inner cost arm now uses estimate_operator_cardinality not estimate_plan_cost (row counts not cost units). Tests: where_clause_index_seek_test.rs (9) + composite_index_seek_test.rs (4 E2E via CREATE CONSTRAINT IS NODE KEY) + planner unit tests. Verified both passes: full workspace green modulo the KNOWN Windows Tantivy fulltext flakes (writer_honours_max_batch_capacity_trigger, fulltext_ranking_regression — both pass in isolation, NOT my regression). PROCESS NOTE: the implementer agent for #7 first did zero-work, then reverted mid-way, then re-implemented — verify via git EVERY time. Filed follow-up phase0_fix-where-clause-index-seek-extensions (range/IN/prefix seek, $param seek, EXPLAIN/PROFILE display planners don't wire property_index/composite_index — pre-existing).

USER REQUEST — CHANGELOG restructured (commit 486c9485): CHANGELOG.md was 3375 lines; split each released version ([2.6.0]..[1.0.0], 23 versions) into docs/changelog/<version>.md + docs/changelog/README.md; CHANGELOG.md now = preamble + [3.0.0] Unreleased (full) + a dated index table linking to the per-version files (213 lines). Lossless (awk split, sum of parts == 3375). NEW ENTRIES still go in CHANGELOG.md's [3.0.0] section. Backup at scratchpad/CHANGELOG.backup.md.

SESSION TALLY (6 fixes landed+archived + 1 urgent + 1 docs restructure): plan-reorder-drops-predicates 3b13f530, varlength-multi-reltype-dropped c76e41c5, tempdir-record-store-leak a3c10a49 (urgent), where-predicate-reparse-precedence e9280c9b, optional-match-var-scoping 877b81df, where-clause-index-seek f8eb857e+67eba262, CHANGELOG restructure 486c9485. NEXT in cluster: skip-dropped-on-pattern-queries [groundwork f95d4458] -> relationship-write-clauses-dropped -> multi-hop-count-star -> cypher-unbounded-alloc-oom. Follow-ups queued: loadcsv, shortestpath, where-case-comprehension, engine-temp-dir-catalog-lock-residual, where-clause-index-seek-extensions.

### 2026-07-23
Completed + archived phase0_fix-cypher-unbounded-alloc-oom (CRITICAL, user-picked out of sequence). Commit 9ea312ee (fix) + 96a3372d (archive). 5 bounded DoS/OOM guards: (1) range() — MAX_RANGE_ELEMENTS=2M cap before Vec::with_capacity + fully-checked count (unsigned_abs for i64::MIN) + checked_add loop (kills the release-build infinite loop from wrapping i+=step); (2) lpad/rpad — MAX_PAD_LEN=1M cap + running char-count (kills O(n²)); (3) var-length path — max_length.min(MAX_VAR_LENGTH_PATH_DEPTH=64), mirrors quantified_expand MAX_QPP_DEPTH; (4) materialize_rows_from_variables — checked_mul + cartesian_product_max_bytes budget guard (function now returns Result -> rippled to ALL executor operator callers: union/unwind/join/expand/filter/project/create/scan/aggregate/dispatch); (5) BYTES — reject_oversize_base64 pre-cap (s.len() > MAX_BYTES*4/3+4) before B64.decode at value+param sites. PROCESS: the implementer agent thrashed HARD — implemented the 5 guards correctly but left the build BROKEN (materialize Result ripple missed union.rs+unwind.rs+a helpers.rs test) and wrote ZERO tests. I finished the compile propagation MYSELF (added ? at the missed call sites) and wrote ALL 10 regression tests myself: tests/cypher/unbounded_alloc_guard_test.rs (8 query-level) + 2 unit (materialize budget in helpers.rs, bytes pre-cap in bytes.rs). SAFETY: never ran the pre-fix OOM/hang repros (would crash the machine — user is memory-constrained); confirmed defects by code inspection, tested only fixed behavior (errors before allocating, all tests <1s). Full workspace green CARGO_EXIT=0 0 failed (no flake this run); clippy+fmt clean. CHANGELOG + cypher-subset.md (4 sections) updated. USER FEEDBACK: keep tasks.md checkboxes updated AS I GO, not just at archive time — now doing that. SESSION TALLY (7 fixes + 1 urgent + 1 docs restructure): plan-reorder 3b13f530, varlength-multi-reltype c76e41c5, tempdir-leak a3c10a49, where-precedence e9280c9b, optional-match-scoping 877b81df, where-clause-index-seek f8eb857e+67eba262, cypher-unbounded-alloc-oom 9ea312ee; CHANGELOG restructure 486c9485. NEXT in cluster (if resumed in sequence): skip-dropped-on-pattern-queries[groundwork f95d4458] -> relationship-write-clauses-dropped -> multi-hop-count-star. Follow-ups queued: loadcsv, shortestpath, where-case-comprehension, engine-temp-dir-catalog-lock-residual, where-clause-index-seek-extensions.

