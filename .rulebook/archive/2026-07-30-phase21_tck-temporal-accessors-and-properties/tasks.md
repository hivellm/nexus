## 1. Implementation
- [x] 1.1 property-access dispatch on temporals — eval/projection/core.rs PropertyAccess arm routes tagged temporals to the new accessor table (temporal_kind detection); non-temporal Objects keep the exact prior extract_property path (regression-tested); unknown accessor / kind mismatch → Null (no TCK scenario expects an error — verified by corpus grep); marker-collision degrades to Null, never panics
- [x] 1.2 derived component accessors — new eval/temporal_accessors.rs (single shared table): date year/quarter/month/week/weekYear/ordinalDay/weekDay+dayOfWeek/dayOfQuarter/day; time hour..nanosecond; offset/offsetMinutes/offsetSeconds/timezone (time+datetime, named zone preferred over rendered offset); epochSeconds/epochMillis (datetime, verified against Temporal5 scenario 6: 469020674/469020674645); duration totals (years..nanoseconds) + remainder variants (...OfYear/OfQuarter/OfMonth/OfWeek/OfDay/minutesOfHour/secondsOfMinute/...OfSecond) — totals-vs-remainder split derived from Temporal5 literal tables (property access = totals for months/minutes/seconds; the pre-existing top-level function surface returns remainders and is untouched, still pinned by the compatibility suite); quarterDay dropped (absent from TCK corpus and real Neo4j surface); negative durations truncate toward zero (unit-pinned). Independent review APPROVE — every numeric field of all 7 Temporal5 scenarios hand-verified incl. ISO weekYear boundary (1984-01-01 → weekYear 1983, week 52). NOTE: Temporal5's own scenarios stay TCK-red until phase21_tck-create-function-call-properties (fixtures die at setup) + phase21_tck-temporal-projection-and-store (stored temporals read back as ISO strings) land — accessor implementation is complete and ready

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — CHANGELOG entry; module docs cite Temporal5.feature as the value source without overclaiming end-to-end conformance
- [x] 2.2 Write tests covering the new behavior — 10 unit tests in temporal_accessors.rs (each keyed to a literal Temporal5 row + negative-duration sign convention) + 2 in temporal_value.rs (offset/zone helpers) + 10 integration tests in tests/cypher/temporal_accessor_property_test.rs (inline WITH construction, sidestepping the CREATE fixture gap; includes unknown-accessor-null and non-temporal-map-unaffected controls)
- [x] 2.3 Run tests and confirm they pass — lib accessors 10/10 + temporal_value 21/21; cypher 499+/0 fail; compatibility 245/245 (function surface untouched); clippy -D warnings clean; rustfmt clean
