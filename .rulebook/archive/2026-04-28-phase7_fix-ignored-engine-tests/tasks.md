## 1. Implementation
- [x] 1.1 Located both `#[ignore]` markers at `crates/nexus-core/src/engine/tests.rs:170` (`test_engine_default`) and `:178` (`test_engine_new_default`). Both carried a placeholder comment about default-data-dir parallel-test conflicts.
- [x] 1.2 No hard-coded paths to replace — `Engine::default()` and `Engine::new_default()` already delegate to `Engine::new()`, which has used `tempfile::tempdir()` since at least 1.13.0. The `#[ignore]` markers were stale carry-over from a pre-tempdir implementation. Added an explanatory block comment above the two tests so future readers do not re-add the ignore.
- [x] 1.3 Removed both `#[ignore]` attributes and the matching placeholder comments.
- [x] 1.4 Ran `cargo +nightly test -p nexus-core --lib engine::tests` under default parallelism. Both formerly-ignored tests now run and pass; lib test count moved from `91 passed / 2 ignored` (pre-change) to `93 passed / 0 ignored` (post-change). The two `engine::tests::constraint_enforcement_*` failures observed under parallel scheduling are pre-existing (reproduced with my changes stashed); they belong to a separate parallel-isolation surface (constraint-registry shared state) and are out of scope for this task.
- [x] 1.5 `crates/nexus-core/src/testing/TestDatabase` exists but is unnecessary here — `Engine::new()` already provides per-instance tempdir isolation, which is what the two tests need. Refactoring to `TestDatabase` would add indirection without correctness benefit.
- [x] 1.6 Audited the whole crate for other `#[ignore]` markers with the same placeholder-comment pattern. Remaining ignores fall into three legitimate buckets: (a) perf / stress / soak tests run manually (e.g. `tests/graph_storage_engine_validation_test.rs:222 test_performance_relationship_creation_throughput`, `:508 test_stress_many_relationship_types`); (b) `query_cache.rs::test_intelligent_invalidation` / `test_cache_expiration` / `test_cache_invalidation` / `test_cache_statistics` carry a known `FIXME: Deadlock issue with RwLock` belonging to a separate concurrency bug (separate task warranted); (c) `wal/async_wal.rs::test_batch_size_limit` carries a known timing-issue note around async flushing (separate task warranted). None of the remaining ignores match the "default data dir / parallel tests" pattern this task targets.

## 2. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 2.1 Update or create documentation covering the implementation — explanatory block comment above the two tests at `crates/nexus-core/src/engine/tests.rs` documents why the ignore markers were removed; CHANGELOG entry under `[1.2.0]` `### Fixed — phase7_fix-ignored-engine-tests`.
- [x] 2.2 Write tests covering the new behavior — the two formerly-ignored tests (`test_engine_default`, `test_engine_new_default`) ARE the test surface; they now run on every `cargo test -p nexus-core --lib` invocation.
- [x] 2.3 Run tests and confirm they pass — `cargo +nightly clippy -p nexus-core --all-targets --all-features -- -D warnings` and `cargo +nightly fmt --all` both clean. Both formerly-ignored tests pass under default parallel scheduling.
