name: Rust Lint

on:
  push:
    branches: [master, main, develop]
    paths:
      - "crates/nexus-core/**"
      - "crates/nexus-server/**"
      - "crates/nexus-cli/**"
      - "crates/nexus-protocol/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".cargo/**"
      - ".github/workflows/rust-lint.yml"
      - "README.md"
      - "CHANGELOG.md"
      - "scripts/ci/check_version_consistency.sh"
  pull_request:
    branches: ["**"]
    paths:
      - "crates/nexus-core/**"
      - "crates/nexus-server/**"
      - "crates/nexus-cli/**"
      - "crates/nexus-protocol/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".cargo/**"
      - ".github/workflows/rust-lint.yml"
      - "README.md"
      - "CHANGELOG.md"
      - "scripts/ci/check_version_consistency.sh"

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Install minimal nightly (for fmt)
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt

      - name: Install minimal stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: actions/checkout@v6

      - uses: Swatinem/rust-cache@v2
        with:
          key: clippy
          cache-all-crates: false
          save-if: ${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || github.ref == 'refs/heads/develop' }}

      - name: Check version-string consistency (workspace ↔ README badge ↔ CHANGELOG)
        run: bash scripts/ci/check_version_consistency.sh

      - name: Check code formatting
        run: cargo +nightly fmt --all -- --check

      - name: Check cargo clippy warnings
        run: cargo clippy --workspace -- -D warnings

      - name: Check cargo clippy warnings for all targets
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Check cargo clippy warnings for all targets and features
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Clean after lint
        if: always()
        run: cargo clean
