# Build artifacts from the language cells (regenerated by the matrix).
clients/csharp/bin/
clients/csharp/obj/
clients/rust/target/
clients/*/node_modules/

# Latest run record, rewritten on every `run-matrix.py --json`.
last-run.json
